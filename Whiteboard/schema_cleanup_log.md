# Schema Cleanup Log

Running log of Art 04 §6.1–§6.3 (Card Data Schema — field groups, enum vocabularies, Modifier Subclass Field Constraints) issues, questions, and normalization candidates surfacing during the 09-16 design review pass. Scope extended S141 from ModReactCard-only to the CA/PA review phase — same log, same format.

**Working premise (Andy, S141):** The CA/PA review pass (and design review passes generally, per [[feedback-review-pass-scope]]) is scaffold + flag, not fix. Findings logged here are flagged for a later normalization/fix pass, not resolved in place during the review itself.

**Working premise (Andy, S137):** §6.1–§6.3 reflect what was needed when drafted, not a locked ceiling. A card/schema mismatch is an open question in either direction — the card may need to change, or the schema section may be behind the pattern that real content now requires. Don't default to "the card is wrong."

**Working method (Andy, S137):** Don't go hunting for extra examples to force a decision early — let them surface organically while working through the full ModReactCard set (all rings + all factions). Wait until that full landscape has been reviewed before proposing any schema normalization — the language different cards use to express the same underlying need is visibly inconsistent even in the handful of examples pulled so far, and the shape of the real fix will be clearer once the whole set has been seen. Until then: keep logging and *categorizing* — what is each card's spec language actually trying to accomplish that the schema doesn't natively support — rather than solving as we go. No §6 edits until Andy calls it.

Each entry: the category of underlying need, which cards surfaced it, the inconsistent language each one reached for, and current status (open / evidence-gathering / ready-to-propose / resolved).

---

## Open Items

### 1. Portrait validity — ModReactCard should NOT be hardcoded to `None`

**Surfaced by:** STD.MOD.98 review (Ring 1 ModReactCard pattern-setter). Nearly proposed extending §6.2's Modifier Subclass Field Constraints table to mark `portrait: None` for ModReactCard the same way it already does for ModBattleCard (any-faction-committable, no doctrine to score).

**Resolution (Andy, S137):** Don't hardcode. ModReactCard is closer to an action card than ModBattleCard/ModActionCard — per §6.1's own framing, it's "the only subclass that routinely carries real Layer/Function/Subject (it's action-like)." Keep `portrait` genuinely open per §6.2 (live/per-card) and assess Portrait implications on a per-card basis during review, same as any Standard/faction card.

**Status:** Direction locked. Not a schema edit — the schema already permits per-card Portrait on ModReactCard (§6.2 lists it as "—", not None); the correction is to actually *do* the per-card assessment during review instead of defaulting every stub to `portrait=None` unexamined. Applies going forward to all 36 Ring ModReactCard cards and the faction ModReactCard sets.

---

### 2. Category: expressing "what ends a standing condition" — no consistent schema language for it

**Surfaced by:** Ring 1 ModReactCard batch (98–109) — `persistence`/`persistence_condition`/`persistence_effect` absent from all 12 specs, which raised the question of whether these fields should be schema-required explicit per-card, or implicit-default for the common case. Andy's response reframed the question one level deeper.

**The deeper issue (Andy, S137):** Some ModReactCards — especially in the faction sets — create a standing condition (`persistence = Seasonal` or `Permanent`) that itself clears via its own trigger, distinct from the trigger that fired the card in the first place. Concrete example pulled: **DIR.MOD.9 Fiscal Sanction** —
- Firing trigger: `standing_marker.decreased(faction=Any)` — fires the card, creates the sanction.
- Clearing "trigger": `persistence_condition = faction(trigger.faction).pays(2, resource.native, to=Reservoir)` — this is written into a field whose declared semantics (§6 Field Groups) are `BoolExpr | None`, continuously evaluated, "card discarded immediately when False." But what's actually being expressed is an *event* (the sanctioned faction pays the fine), not a continuously-true/false board state. The field is being used as a trigger wearing a BoolExpr's clothes.

**Open question:** Should the schema normalize two distinct trigger-type fields on persistent cards — (1) the play/fire trigger (already `trigger`), and (2) a clearing trigger for whatever standing condition the card leaves behind — rather than overloading `persistence_condition` to do double duty as both a state predicate and an event watcher? Related: does this mean `resolution_type`/explicit `persistence` lines are only meaningfully "required per card" for the Seasonal/Permanent minority, and Immediate/Transactional should be the implicit default for ModReactCard unless a card states otherwise (would eliminate the appearance of a 130-card compliance gap that's mostly just the common case going unstated)?

**Additional examples pulled (S137) — pattern confirmed, not a one-off:**

- **GUI.MOD.10 Contractor's Favor** (`persistence=Seasonal`): `persistence_condition=None`, `persistence_effect=None` — the actual clearing rule ("clears at Phase 21 End of Quarter regardless of outcome") exists only as a Python comment below the `success` line. Not encoded in any field at all.
- **SYN.MOD.6 Bounty Contract** (`persistence=Seasonal`): `persistence_condition`/`persistence_effect` omitted entirely. Clearing logic (Capital transfers when the target PA resolves, either direction) is buried inside the `success` field's prose string.
- **NET.PA.3 Live Coverage** (PublicAct, not ModReactCard, but same shape and useful cross-reference): `persistence_condition` is a prose string describing an *event* ("target complies for one Covert Dispatch → clears, or Quarter end"); `persistence_effect` is a separate prose string describing an ongoing per-Month procedural obligation. Same event-wearing-a-BoolExpr's-clothes issue as DIR.MOD.9, just in English instead of pseudo-code.

Four cards (DIR.MOD.9, GUI.MOD.10, SYN.MOD.6, NET.PA.3), four different improvised encodings of "what ends this standing condition," none actually using `persistence_condition` as §6 defines it (continuously-evaluated `BoolExpr`, discard on False). This is a real, repeated authoring gap — every card that needed this reached for something ad hoc instead of a clean schema mechanism, which is itself evidence no clean mechanism currently exists.

**A candidate idea surfaced while looking at these four** (not a proposal — just a shape worth watching against future examples): the "clearing" half of each of these reads as an *event* (a `TriggerExpr`, same vocabulary as the firing `trigger`), not a continuously-evaluated `BoolExpr` — none of the 4 examples actually needed a state predicate. Worth checking whether that holds or breaks as more Seasonal/Permanent cards turn up.

**Status: Closed S147.** Full corpus-wide check before deciding: scanned all 25 Seasonal/Permanent cards in the corpus, classified every populated `persistence_condition` by actual shape. The candidate idea didn't hold universally — found a real split, not a uniform pattern: **STD.CA.11, SYN.PA.3, DIR.PA.3** are genuine continuous state predicates (correctly using the field as designed); **DIR.MOD.9, DIR.PA.5, DIR.PA.6, NET.PA.3, SYN.MOD.6** are one-time events wearing a `BoolExpr`'s clothes; **GUI.MOD.10** turned out not to need a clearing condition at all — its documented behavior ("clears at Phase 21 regardless") is just the default Seasonal expiry already implied by `persistence`, previously undocumented in any field (only a stray Python comment), now confirmed and closed without new content. Andy's direction: don't force a single interpretation — add a second field for the event case rather than overload one field for two jobs. New field `persistence_clearing_trigger: TriggerExpr | None` added to Art 04 §6.1/§6.2 (`Part1_Core.md`), alongside the existing `persistence_condition: BoolExpr | None` (now scoped explicitly to continuous predicates only) and `persistence_effect: MutationExpr | None`. **"Standing Condition" locked as the canonical descriptive term** (Andy's call — it was already de facto standard across `ref_card_types.md`, `design_reference_card_system.md`, and 12+ Directorate card entries, just never formally confirmed; `persistence` stays the schema/field-family name, no renaming). The 5 confirmed event-shaped cards migrated `persistence_condition`→`persistence_clearing_trigger`; content preserved, 2 flagged as still using an unconfirmed `.pays(...)` TriggerExpr form (separate open question, not this item's scope) and 2 (NET.PA.3, SYN.MOD.6) left as prose pending structured syntax. Monolith regenerated; fence/`Card()` counts verified clean. Full detail: PM02 L300.

---

### 3. Category: TriggerExpr parameter gaps — `.removed()` forms used with `ring=` but only `district=` is confirmed

**Surfaced by:** STD.MOD.100 (`presence_chip.removed(faction=opponent, ring=1)`) and STD.MOD.106 (`presence_chip.removed(faction=holder, ring=1)`), Ring 1 ModReactCard batch.

**The gap:** §6.3's confirmed TriggerExpr vocabulary lists `presence_chip.placed(faction=X, district=Y, ring=Z)` — both `district` and `ring` available — but `presence_chip.removed(faction=X, district=Y)` — district only, no `ring` documented. Two Ring 1 cards use `ring=` on the `.removed()` form anyway. Plausible this is just an oversight in how §6.3 was written (symmetric `ring` support on `.removed()` is a reasonable, low-risk assumption), but it's not yet confirmed as written.

**Status:** Closed (PM05 04-n195, Phase 4 item 1, S149). Confirmed: `ring=` is valid on all `.removed()` TriggerExpr forms, symmetric with `.placed()`. Documented in §6.3 (Part1_Core.md).

---

### 4. Full-landscape synthesis (S137) — schema-as-a-whole recommendations

**Scope note:** this synthesis is grounded in the *taxonomy and schema-field usage* pass across all 36 Ring ModReactCards + all 5 factions' ModReactCard decks (~90 cards). It is NOT a full design review (Portrait/Card Story/22-row checklist) of the faction decks — only Layer/Function/Subject taxonomy was assigned there. Recommendations below are about schema structure and field-usage patterns, not per-card design quality.

**A. No confirmed MutationExpr vocabulary exists, unlike TriggerExpr.** Verified directly against canonical Art 04 §6 (Part1_Core.md), not just the Whiteboard reference: `MutationExpr` is a declared field type (§6 Field Groups, success/successcrit/fail/failcrit/persistence_effect) but there is no enumerated list of confirmed valid call forms the way §6.3 enumerates TriggerExpr forms (`presence_chip.placed(...)`, `structure_block.removed(...)`, etc.). Effect-side authors improvised freely: `arbiter.place`, `arbiter.remove`, `arbiter.deliver`, `arbiter.modify`, `arbiter.shift`, `arbiter.draw_modifier`, `arbiter.apply_modifier`, `arbiter.register_battlefield_modifier`, `arbiter.mark_acceptance`, `faction(X).resources.add`, `faction(X).standing.add/shift` — a wide, ungoverned set. This is one contributing factor (not the sole cause) behind: the Copy-vs-Add and Redirect-vs-Remove corrections made this session, and GUI.MOD.10's `register_battlefield_modifier` having no taxonomy home at all. Candidate direction: extend §6.3 with a confirmed MutationExpr vocabulary, mirroring the TriggerExpr treatment.

**B. Persistence/clearing-condition — see Item 2 above.** Now that the full landscape has been seen: this is real but low-frequency (4 examples total across ~90+ cards: DIR.MOD.9, GUI.MOD.10, SYN.MOD.6, NET.PA.3 — no new instances surfaced in the Guild/Network/Syndicate taxonomy pass, though that pass wasn't a full design review so more may exist unexamined). Every Seasonal/Permanent card that needed to express "what clears this" reached for something ad hoc — prose in `success`, a Python comment, an event dressed as `persistence_condition`'s `BoolExpr`. The candidate shape floated in Item 2 (a second `TriggerExpr`-typed clearing-trigger field, distinct from the firing `trigger`) still looks right against these 4, but is not a proposal — Andy's call once he's ready to look at it.

**C. Portrait-on-ModReactCard — direction already locked (Item 1), not re-opened here.** Note for accuracy: this session's faction taxonomy pass did not include Portrait assessment (out of scope, taxonomy-only), so Item 1's "assess per card" direction has only actually been executed on the Ring set so far, not the faction decks.

**D. Undeclared vs. explicit-None fields.** Several older cards (NET.MOD.1/2, GUI.MOD.1, the GHO.MOD.9/10/11 fossils) don't just leave `layer/function/subject = None` — they omit the fields from the `Card()` call entirely. A completeness checker can't distinguish "reviewed and correctly None" from "never touched" when the field isn't even declared. Candidate direction: require all schema fields explicitly declared (even as `None`) on every card as a baseline completeness gate.

**E. Nothing currently flags missing ModReactCard taxonomy as a distinct gap.** ~48 of ~54 faction ModReactCards had `layer=None` going into this session, undetected until a manual read-through. Candidate direction: extend whatever completeness/audit tooling exists (`v_card_mechanical_alignment` or similar) to flag `type=ModReactCard AND layer IS NULL` as its own check, so a future stub batch doesn't silently reintroduce the same gap.

**F. `PublicStanding` vs `StandingMarker` — already-documented rule, no enforcement.** `ref_taxonomy.md` already states not to use `PublicStanding` as a card subject (corrected S126); this session found and fixed 12+ cards still using it anyway (missed because nothing checks for it). Same shape as D/E: the rule exists, the enforcement doesn't. Not a new schema question — folds into whatever audit-tooling recommendation Andy wants to act on for D/E.

**Status (S150):** **B already closed** — item #2 (S147) added `persistence_clearing_trigger` exactly as B's candidate shape proposed; not reopened here. **C already resolved** — item #1's direction locked, not reopened. **A closed (this session).** Andy's direction: "we're going to need to address this anyway, might as well do it now" — full enumeration, not organic. Corpus scan found 56 distinct `arbiter.*`/`game.*` call forms (293 usages, 26 single-use) plus a receiver-chain mutation surface with 3 competing shapes for resource mutation and 2 for presence/structure token mutation. Both consolidated:

**Resource mutations (Mandate/Capital/Findings/Capacity/Native), 40 instances.** Three shapes found: `resource.faction(X).TYPE.add(n)` (8), `faction(X).resource.TYPE.add(n)` (9), `faction(X).resources.add(n, TYPE)` (23, the plurality). Andy's resolution: use CostExpr's own confirmed grammar as the model — named resource types are bare, first-class attributes (`Mandate * n` in CostExpr), not wrapped or passed as call arguments. This points at `faction(X).TYPE.add(n)`, matching the 112-instance `standing` baseline, not the plurality-winning `resources.add()` shape (which breaks CostExpr's own "type is first-class" principle). All 40 converted. `.native` kept as CostExpr's contextual qualifier (`faction(X).native.add(n)` when the receiver's own native type applies; `faction(X).native(faction=Y).add(n)` / `.native(district=D).add(n)` for the cross-reference case, e.g. crediting one faction's pool typed by a different faction's or district's native resource). New confirmed §6.3 entry written documenting the full grammar and citing this item.

**Presence/structure token mutations, 30 instances.** Two receiver-chain shapes competed (`district(D).faction(F).FIELD.add(n)` and `faction(F).FIELD_token.add(D, n)`), but investigation found a third, already-confirmed option: `arbiter.place`/`remove(component, district=, faction=, count=)` (schema_cleanup_log #6) — already dominant for presence (39 instances vs. 19 combined) and already the officially locked-in form. Andy's ruling: converge both presence and structure onto this existing primitive rather than confirm either receiver-chain shape. All 30 instances converted (19 presence + 9 structure, plus 2 stragglers caught on a follow-up sweep using non-matching call shapes — `district.each(target)` harmonized to the card's own established `district.each_target` bare-reference convention, then wrapped in `arbiter.place()`). §6.3's existing `arbiter.remove(presence_chip,...)` entry extended to document the full consolidation and structure_block's inclusion.

**Verification:** fence count (776) and `Card()` count (386) unchanged throughout both sweeps; monolith regenerated after each stage. Stale checklist/prose mentions of retired shapes corrected where found (2 checklist rows, 2 inline comments).

**D/E/F closed (S150).** Built `tools/audit_card_corpus.py` — parses every `Card()` instance directly from `V1/04___Card_System.md` (source of truth, not the DB mirror) and checks all three in one pass.

- **D — not built as a literal gate; closed as superseded by E.** The candidate direction ("require every field explicitly declared, even as None") doesn't fit the corpus's own documented convention: fields are deliberately omitted when a stated default applies (e.g. Part1_Core.md's own note that `on_discard = None` is implicit unless a card deviates), and `is_unique`/`deck_limit` are undeclared on all 386 cards by design. Checking the 6 cards D's own text named as motivating examples (NET.MOD.1/2, GUI.MOD.1, GHO.MOD.9/10/11) shows all 6 already have complete `layer`/`function`/`subject` — the concrete risk D was reacting to (taxonomy silently skipped) is the one category where full declaration genuinely matters, and that's exactly what E checks. Andy confirmed closing D this way rather than mandating a corpus-wide rewrite against established convention.
- **E — closed.** Script found only 2 `type=ModReactCard AND layer IS NULL` hits corpuswide: STD.MOD.1 (Overture) and GUI.MOD.10, both already documented as `Issued ModReactCard — taxonomy excluded §11.1` in Part1_Core.md's status table — legitimate, signed-off exceptions, not gaps. Confirms the ~48-card S137 gap this item was written about is now fully closed. New `card_status.mod_subtype` ENUM('Action','Battle','React') column added and backfilled from the corpus's own `type` field (269 MOD-type rows: 132 Action/44 Battle/93 React) — the DB previously collapsed all three ModAction/ModBattle/ModReact subclasses into one `MOD` bucket with no way to distinguish them, which is what made E's literal "extend the DB view" ask impossible before this. New view `v_card_taxonomy_gaps` flags any future regression (excludes the 2 documented exceptions by card_id).
- **F — closed, already clean.** 0 cards use the retired `subject=PublicStanding` value corpuswide. Folded into `v_card_taxonomy_gaps` as a second check (`gap_type = 'Retired PublicStanding subject'`) for future regression protection.
- **Bonus catch:** the E/F cross-check surfaced one genuine `card_status` sync gap unrelated to D/E/F — **GD-01 (Grant Deed)** had `layer`/`function`/`subject` all NULL in the DB despite the corpus having `Territory`/`Add`/`StructureBlock` set; fixed by direct sync from the corpus. GD-01 also lacks an explicit `card_id=` kwarg in its `Card()` call (only `id=`) — noted but not fixed, out of scope for this item.

---

### 5. Category: TriggerExpr `faction=Any` — no confirmed semantics for self-inclusion

**Surfaced by:** DIR.MOD.1 Riot Squad (S138, Directorate ModReactCard review). Trigger is `presence_chip.placed(faction=Any)`. Sibling card DIR.MOD.7 (same author, same session) uses `faction=opponent` for a structurally identical shape. §6.3's confirmed TriggerExpr vocabulary documents `faction=X` parameterization but never states whether `Any` includes the reacting card's own owning faction or is implicitly "any other faction." No card-level fix applied — flagged as a vocabulary gap, not a DIR.MOD.1-specific bug, since the ambiguity could recur anywhere `faction=Any` is used on a self-triggering React.

**Status: Closed S147.** Full-corpus re-grep before deciding (all 8 Art 04 Part files, not relying on the cached S138 count) — 9 confirmed instances across 3 factions: DIR.MOD.1/3/9 (Directorate), GHO.MOD.2/6/7/8 (Ghost), NET.MOD.6/10 (Network). One author (NET.MOD.1) had already hand-patched the ambiguity locally with an explicit `except=Network` — real evidence the gap needed closing, not just a hypothetical. Andy's call: **`faction=Any` is inclusive of the reacting card's own faction by default.** §6.3 (Part1_Core.md TriggerExpr block) updated: grammar extended to `component[.scope][.attribute].change(faction[, except])`, semantics note added stating inclusive-by-default with `except=X` as the explicit-exclusion mechanism (precedent: NET.MOD.1). Of the 9 instances, 8 resolve as confirmed-harmless costed no-ops or intentional design (no card change needed) — DIR.MOD.1/3, GHO.MOD.2/6/7/8, NET.MOD.6/10, all Trigger-validity rows flipped ⚠→✓. The 9th, **DIR.MOD.9**, is the sharp exception (self-fire is actively self-harmful, not a no-op — see item 11) — its trigger gained an explicit `except=Directorate`. All 9 cards' Design Rationale and checklist notes updated to reflect resolution.

---

### 6. Category: `arbiter.remove(presence_chip, ...)` doesn't distinguish protected vs. removable chips

**Surfaced by:** DIR.MOD.1 Riot Squad (S138). The MutationExpr call `arbiter.remove(presence_chip, district=X, faction=Y, count=1)` has no way to express "not a Deployment Marker's temporary chip" — GR 8.3a is hard: deployment markers are always moved, never removed. Any card using this same removal shape (DIR.MOD.1/2/3 all do) is silently ambiguous about whether it could target protected marker-linked chips. Same root cause as Item A (no confirmed MutationExpr vocabulary) — this is a concrete instance of that gap rather than a new category.

**Status:** Closed (PM05 04-n195, Phase 4 item 2, S149). Original framing corrected on Andy's direct pushback — there's no "marker-linked chip"; Presence Token (DB:1) and Deployment Marker (DB:2) are separate physical components. Confirmed: `arbiter.remove(presence_chip, ...)` can never target a Deployment Marker, full stop — the marker "counts as 1 Presence Token for all purposes" (Art 02 §6) only for counting/influence-level purposes, not as a valid removal target. Documented in §6.3 MutationExpr section. DIR.MOD.1/2/3 and GHO.MOD.7/8's "Supported by components" checklist rows updated from ⚠ to ✓.

---

### 7. Category: Portrait `flat` entry on a non-submitting (target) faction — schema-permitted, doctrinally unexamined

**Surfaced by:** DIR.MOD.2 Capital Suppression (S138). `portrait = {Directorate: submitter=+1, Syndicate: flat=-1}` — the `Syndicate: flat=-1` entry moves a faction's Portrait as a direct consequence of a rival's card resolving against them, not as a result of Syndicate's own choice or action. Schema-valid per L131 (`flat` documented as "fires on resolution regardless of submitter — faction-specific cards only"), so this isn't a vocabulary gap the way Items 5/6 are. The open question is doctrinal: Portrait Principle 11 ties movement to an action that strongly expresses *that faction's own* doctrine — does having something done to you count, or should target-faction portrait entries require the target's own agency? If the answer is "no, targets shouldn't get portrait entries for being acted upon," this could affect any existing card using the same pattern (not audited yet — this is the first instance flagged, not a confirmed one-off).

**Status:** Split into two questions, S149/S150. **Q1 (submitter's-own-entry using `flat=` instead of `submitter=`) — Closed S150.** Andy's ruling (S149, PM05 04-n199): fix all submitter-own entries to `submitter=`. Corpus-wide audit (S150, beyond the 5 originally named) found **7 total instances**: SYN.CA.7, SYN.CA.10, SYN.CA.11, SYN.CA.12, SYN.PA.3 (the 5 originally flagged) plus **SYN.MOD.1 and SYN.MOD.11** (found in the audit, same pattern — single-entry `{Syndicate: flat=+N}`). All 7 corrected to `submitter=`. Checklist Portrait-validity rows updated on each. Fence count (776) and `Card()` count (386) unchanged; monolith regenerated.

**Q2 (target-faction `flat=` entries — does agency matter for Portrait movement) — Closed S150.** Andy's ruling: "Portrait is only based on the action of the current faction player" — no agency, no portrait entry, full stop. All 7 target-faction `flat=` entries removed across the 4 cards: DIR.MOD.2 (`Syndicate: flat=-1` dropped, leaving `{Directorate: submitter=+1}` only), SYN.CA.10/CA.11/PA.3 (`Network: flat=-1` + `Directorate: flat=-1` dropped from each, leaving `{Syndicate: submitter=+1}` only). DIR.MOD.2's Design Rationale rewritten to state the ruling directly rather than flag it as open. All affected checklist rows updated ⚠→✓. Zero `PortraitEntry(flat=` instances remain anywhere in the corpus — `flat` as a Portrait field is now fully retired, not just unused on these 4 cards. Fence count (776) and `Card()` count (386) unchanged; monolith regenerated. `flat`'s §6.1/§6.2 field documentation should be reviewed for removal in a future schema pass if no future use case emerges — not done here, out of scope.

---

### 8. Category: `portrait = {}` vs. `portrait = None` for "no portrait effect" — inconsistent across sets, not within Directorate

**Surfaced by:** Directorate ModReactCard review (S138). §6.1's class definition types `portrait` as a plain `dict[Faction, PortraitEntry]` — not `Optional`/`| None`. DIR.MOD.1–9 consistently use `{}` for "no entries" (DIR.MOD.5/6/7/8) vs. a populated dict otherwise — internally consistent within the Directorate set. But the Ring ModReactCard template (STD.MOD.98, reviewed S135) uses `portrait = None` for the same "no effect" case, which is arguably not type-correct against §6.1's declared field type. Two different authoring sessions independently picked different representations for the same empty case; neither is wrong per card, but the set as a whole isn't normalized.

**Status: Closed S148 (PM05 04-n192/Phase 1, PM02 L319).** Full corpus count before deciding (not just the Directorate/Ring sample originally flagged): `portrait = None` — 422 instances (dominant, Ring Modifiers 228 + ~32 per faction file); `portrait = {}` — 89 instances (minority, Directorate/Syndicate highest at 16 each). This reverses the original assumption — the declared type was behind the pattern, not the cards. Andy's ruling: normalize to `None` (the majority form), not `{}`. All 89 `{}` instances swept to `None` across Standard/Guild/Ghost/Directorate/Network/Syndicate/Ring Modifiers. §6.1/§6.2 (`Part1_Core.md`) `portrait` type declaration updated from `dict[Faction, PortraitEntry]` to `dict[Faction, PortraitEntry] | None`. One stale checklist-prose mention (Directorate) comparing "None" against "{}" updated to match. Fence (776)/`Card()` (385) counts unchanged; monolith regenerated.

---

### 9. Category: `where(...)` as a trigger parameter — not in confirmed TriggerExpr vocabulary

**Surfaced by:** DIR.MOD.8 Asset Seizure (S138). Trigger is `public_act.placed_on_frg(target_district=where(faction(Directorate).influence >= Established))`. §6.3's confirmed vocabulary shows `public_act.placed_on_frg(faction=X, ...)` with flat key=value parameters (faction, ring, district) — nothing showing a conditional/filtered parameter built from a `where(BoolExpr)` wrapper. This is a different shape from anything in the confirmed list: instead of matching a specific value, it's filtering by a live board-state condition evaluated at fire time. Possibly fine (the underlying event and condition are both well-defined elsewhere in the schema), but the parameterization pattern itself isn't confirmed vocabulary.

**Status: Closed (S150).** Second look found the actual reconciliation path: `district.where(BoolExpr)` (with a receiver) was already confirmed elsewhere in the corpus as a MutationExpr collection-filter (DIR.PA.7's `move_to=district.where(faction.has_presence)`, DIR.PA.9's `district.adjacent(X).where(ring==...)`, DIR.PA.10's `district.where(influence_tier >= Established)`) — all written this session under item #29/#44. The 5 flagged cards used a *different* shape for the same idea: bare `where(...)` with no receiver, as a raw parameter value (`district=where(...)`). Andy's ruling: confirm the capability, harmonized to the already-established receiver form rather than leaving two grammatical shapes for the same word. All 5 cards (GHO.MOD.2/3/4, DIR.MOD.8, GUI.MOD.5) rewritten `district=where(...)` → `district=district.where(...)` (or `target_district=` for DIR.MOD.8). Added to Part1_Core.md §6.3's TriggerExpr vocabulary as a confirmed parameter form. All 5 cards' checklist "Trigger validity" rows updated ⚠→✓; stale Design Rationale/Status-footer prose describing the old "unconfirmed" status corrected on all 5 plus one additional stale mention. Fence count (776) and `Card()` count (386) unchanged; monolith regenerated.

---

### 10. Category: Intel Token as `cost` — is a discrete, individually-tracked object a valid "fungible resource"?

**Surfaced by:** DIR.MOD.9 Fiscal Sanction (S138). `cost = intel_token(faction=trigger.faction, age__in=[Fresh, Stale]) * 1`. §6 Field Groups defines `cost: CostExpr` as "fungible resources only; PS and presence tiers are not valid cost values." Intel Tokens are individually tracked objects with per-token age/privacy state (Fresh/Stale/Expired, holder-keyed) — not a fungible pool the way native resources are. This may be an established, accepted pattern already (Intel Tokens function as a spendable resource type elsewhere — e.g., SCIF/Deep Cover economies), but it hasn't been checked against the literal "fungible resources only" constraint before. Not fixed — genuinely unclear whether this is a schema violation or whether Intel Tokens should simply be recognized as a second valid cost category alongside native resources.

**Status: Closed S147.** Re-audited corpus-wide before closing (S142's count was stale) — **19 total instances found, 17 live + 2 pre-schema fossils** (GHO.MOD.9/10, excluded per item #20's fossil-scope note, not reconciled). Andy's call: formalize Intel Token as a second confirmed `cost`/`boost` category, and normalize all live instances to one canonical notation in the same pass. New `CostExpr`/`IntelToken(about, status)` vocabulary written into Art 04 §6.3 (`Part1_Core.md`) — `about` unifies the prior `faction=`/`target=`/`keyed_to=` parameter spellings (which faction the token concerns, not its holder — holder is always implicit), `status` unifies `age__in=[...]`/bare `status=` (Fresh/Stale/Expired filter). All 17 live instances migrated to canonical form across Ghost (9: GHO.CA.2/6/9/10×2/12/14/15, GHO.PA.1×2, GHO.PA.3), Standard (2: STD.CA.12, STD.PA.5), Network (1: NET.PA.1), Directorate (3: DIR.CA.5, DIR.PA.8, DIR.MOD.9), Syndicate (2: SYN.CA.7, SYN.MOD.1) plus SYN.MOD.11's restriction field and SYN.MOD.1's restriction field (same `IntelToken()` constructor, not cost, updated for consistency). Every affected checklist row's "open fungibility question" language updated to reflect resolution — rows whose *only* flag was this question flipped ⚠→✓; rows carrying other, unrelated open issues (e.g. STD.PA.5's untyped `resource.faction(acting)` term, item #22; DIR.PA.8's bare-string `success` defect) correctly remain ⚠ for those separate reasons. §6.1 field dictionary's `cost` row updated to list Intel Token as a valid category. Monolith regenerated. Full detail: PM02 L298.

---

### 11. Category: `trigger.faction` self-reference on broad (`faction=Any`) React triggers — same root issue as Item 5, sharper consequence

**Surfaced by:** DIR.MOD.9 Fiscal Sanction (S138). `trigger = standing_marker.decreased(faction=Any)`, then `persistence_effect = PublicAct(submitter=trigger.faction).blocked_at(phase_b)`. If Directorate's own Public Standing drops and Directorate holds a matching Intel token on itself, this card would fire against Directorate itself — sanctioning its own faction, blocking its own PA submission, in exchange for +1 PS to itself. Same category of self-fire ambiguity as Item 5 (DIR.MOD.1), but here the consequence is actively self-harmful rather than merely a no-op, and the card's own design_note narrative ("holding tokens on rivals") implies the intended scope was always `faction=opponent`.

**Status: Closed S147.** Resolved together with item 5 as planned. §6.3 now locks `faction=Any` as inclusive-of-self by default (item 5) — under that default, DIR.MOD.9's self-fire would be a real, active harm (self-sanction, blocked own PA channel), not the harmless no-op the other 8 `faction=Any` instances resolve to. Confirmed the card's own design_note intent was always opponent-only ("holding tokens on rivals"). Fix: added explicit `except=Directorate` to the trigger — `standing_marker.decreased(faction=Any, except=Directorate)`. Design Rationale, Balance, and Trigger validity checklist rows updated to reflect resolution (Trigger validity ⚠→✓).

---

### 12. Category: Invalid `resolution`/`resolution_type` enum values on a legacy (S110) card

**Surfaced by:** GHO.MOD.1 Sleeper Analyst (S138, Ghost ModReactCard review — format-migration case, `structure_pass=0`). Card uses `resolution = Prediction` and `resolution_type = "Conditional"`. Neither is a valid value: confirmed `Resolution` enum (Art 04 §6.3, verified directly against `Part1_Core.md`) is `d100 | Automatic` only; confirmed `resolution_type` vocabulary is `"Probabilistic"` (d100) or `"Transactional"` (Automatic) only. This card predates both confirmed vocabularies (S110, well before the S127+ enum lock-downs) and was never swept to match. The underlying mechanic (Ghost declares a faction name, ARBITER checks it against private Intel Token data, no dice) reads as a deterministic check — likely `resolution=Automatic` once corrected, but that's a content call, not made here.

**Status:** Closed (PM05 04-n195, Phase 4 item 10, S149). Confirmed `resolution=Automatic`, `resolution_type=Transactional` — the mechanic is a deterministic declare-then-verify check, no dice.

---

### 13. Category: Legacy TriggerExpr syntax (`faction(X).places(...)`) predates confirmed §6.3 vocabulary

**Surfaced by:** GHO.MOD.1 Sleeper Analyst (S138). `trigger = faction(opponent).places(PA, with=IntelToken(any), at=Art 03 §9.2.0)` — this construction style (method-call-on-faction-object, embedding an Art-section citation as a parameter) doesn't match any confirmed §6.3 form (which uses `event_noun.verb(param=value, ...)`, e.g. `public_act.placed_on_frg(faction=X, ...)`). Same root cause as Item 12 — a pre-S127 card that was never swept to current vocabulary. The closest modern equivalent is likely `public_act.placed_on_frg(faction=opponent, uses_intel_token=True)` or similar, but the exact reconciliation is a content decision, not made here.

**Status:** Closed (PM05 04-n195, Phase 4 item 10, S149). Confirmed: `public_act.placed_on_frg(...)` gets a new `uses_intel_token=True` filter parameter, added to §6.3. GHO.MOD.1's trigger now reads `public_act.placed_on_frg(faction=opponent, uses_intel_token=True)`.

---

### 14. Category: `public_standing.shifted(direction=...)` — retired trigger term, missed instance

**Surfaced by:** GHO.MOD.5 False Flag (S138). Trigger is `public_standing.shifted(faction=Any, direction=Positive)`. PM05 04-n144 (closed S130) already normalized this exact term to `standing_marker.increased/decreased(faction=X)` — but that sweep's known instances were SYN.MOD.4/5 only. GHO.MOD.5 uses the identical retired form and was missed by the original sweep (it wasn't in the S128 stub batch that 04-n144 was scoped against, or was simply overlooked). Not fixed here — flagged as a gap in an already-closed item's sweep completeness.

**Status:** Closed (PM05 04-n194, Phase 3). Swapped to `standing_marker.increased(faction=Any)` — no other instance found corpus-wide.

---

### 15. Category: `PA_success.where(...)` legacy trigger syntax + `acting` keyword (NET.MOD.1, S106-vintage)

**Surfaced by:** NET.MOD.1 Pirate Transmitter (S138, Network ModReactCard review — format-migration case, `structure_pass=0`). Trigger is `PA_success.where(effect.causes_board_state_change(district))` — same category as items 12/13 (GHO.MOD.1's legacy syntax), a pre-S127 construction that never got swept to confirmed §6.3 vocabulary. Also uses a bare `acting` keyword in `cost`/`successcrit`/`failcrit` (e.g. `resource.faction(acting).exposure`) — **checked S149: this is already-confirmed vocabulary** (Part1_Core.md §6.3 MutationExpr documentation lists `acting`/`faction(acting)` explicitly, mirroring STD.MOD.1 Overture's established `faction(acting)`), not a gap. Only the trigger term itself remains open.

**Status:** Closed (PM05 04-n195, Phase 4 item 11, S149). New general-purpose §6.3 primitive confirmed: `board_state.changed(component=, change=, cause=, faction=, district=, ring=)` — covers any publicly visible, non-procedural game-state change (component ∈ presence_chip/structure_block/standing_marker/deployment_marker/accord/public_act/modifier_card/native_resource/intel_token/target_profile/Any; change ∈ placed/removed/increased/decreased/moved/corrupted/Any; cause ∈ public_act/covert_operation/modifier_card/upkeep/Any — filters by what produced the change, not just what changed; no separate "arbiter" value since ARBITER executes the change but is never itself the cause). Coexists with the itemized single-event forms rather than superseding them. `cause=` was added after catching that the first draft silently dropped NET.MOD.1's original PA-specific gate — Andy's intent is narrower than "any board change": Network specifically capitalizes on a PA's visible consequence ("we're broadcasting what you did"), not a CA/React/Upkeep-caused change. NET.MOD.1's trigger now reads `board_state.changed(component=[presence_chip, structure_block], change=Any, cause=public_act, faction=Any)`.

---

### 16. Category: `status_marker.contested.placed()` — legacy trigger syntax, confirmed vocabulary is `tension_marker.placed`

**Surfaced by:** NET.MOD.9 Bandwidth Override (S138). Trigger is `status_marker.contested.placed()` — §6.3's confirmed vocabulary for this exact board event is `tension_marker.placed` (already used correctly by GUI.MOD.10 and others). This is a different, unreconciled term for the same underlying event, not a new mechanic.

**Status:** Closed (PM05 04-n194, Phase 3). Swapped to `tension_marker.placed()`.

---

### 17. Category: Statement syntax (`-=`) used as a field value, not an expression

**Surfaced by:** NET.MOD.2 Troll Farm (S138). `success = faction(trigger.faction).standing -= 1,` — `-=` is a Python augmented-assignment *statement*, not a value expression; it cannot legally appear as the right-hand side of a `success=` keyword argument the way every other card's `success` field does (a callable mutation like `arbiter.shift(...)` or `faction(X).resources.add(...)`). This is a different, more severe category than the "string literal instead of Expr" fossil gap (04-n174) — it's not even valid pseudo-code, whereas the fossil cards' string literals at least parse as strings. Likely should read `arbiter.shift(standing_marker, faction=trigger.faction, amount=-1)` or `faction(trigger.faction).standing.remove(1)` (matching GHO.MOD.5/STD.MOD.10x precedent), but that's a content decision, not made here.

**Status:** Closed (PM05 04-n194, Phase 3) for its original 2-instance scope: NET.MOD.2 → `faction(trigger.faction).standing.remove(1)`; SYN.MOD.9 → `faction(Syndicate).standing.add(N)` (also corrected in its `arbiter_note`). **Scope was incomplete — see item #55.** The original audit found only these 2 instances; a full corpus grep run under item #51/#55 (S150) found 111 more of the exact same defect across every other faction file. The underlying ruling (`.add()`/`.remove()` is canonical) holds — it just wasn't swept corpus-wide at the time this item closed.

---

### 18. Category: `modifier_card.placed(faction=X)` — unconfirmed trigger term, plus a possible self-triggering loop

**Surfaced by:** NET.MOD.8 Frequency Splitter (S138). `trigger = modifier_card.placed(faction=Network)` isn't in the confirmed §6.3 TriggerExpr list. More significant: the design_note calls this a "chain enabler" that "replaces itself," but the trigger fires on *any* Network modifier card being placed — including, potentially, this card's own resolution. If so, this could be a self-sustaining recursive loop (fire → draw another modifier → possibly re-trigger → fire again) rather than a bounded chain. Not confirmed as a bug — may have an implicit once-per-Month limiter not written into the spec — but the mechanic as specced doesn't rule out the loop reading.

**Status:** Closed (PM05 04-n195, Phase 4 item 12, S149). Trigger now reads `board_state.changed(component=modifier_card, change=Placed, faction=Network)`, using the new §6.3 primitive (item 15/11's `board_state.changed`). Self-triggering chain confirmed intentional and unbounded (Andy's ruling) — naturally self-limiting since it can't outrun the finite Network modifier deck; no explicit limiter needed.

---

### 19. Category: A ModReactCard that never discards has no fitting Persistence value

**Surfaced by:** SYN.MOD.9 Goodwill (S138). Design_note explicitly states "Card does not discard — remains active for further triggers." None of the 4 documented Persistence values fit this: `Immediate` is fire-and-consume; `Transient`/`Seasonal` clear at Month/Quarter end; `Permanent` requires an explicit `persistence_condition` that, when False, discards the card — but this card is meant to remain active indefinitely, forever, with no clearing condition at all (it's a standing reusable ability, not a standing condition awaiting removal). This is a different shape than the persistence/clearing-condition gap already logged (item 2/B, "what ends a standing condition") — item 2/B is about cards that DO eventually clear but whose clearing logic is expressed inconsistently; this card is about a card that structurally never clears, which the 4-value enum doesn't model at all.

**Status:** Closed (S149) — not a schema gap, the card's design_note was wrong. Andy's ruling: React cards are discarded and permanently removed from the game by default once resolved (a rule that existed nowhere in Art 03 §18 until this session — added as §18.2.2). SYN.MOD.9's "does not discard — remains active for further triggers" claim contradicted that default and wasn't an intentional exception Andy wanted to keep — corrected to fire once and discard like any other React. No schema change needed; `persistence` doesn't apply to hand-held Reacts regardless (it governs board-placed CA/PA duration, per §6.2).

---

### 20. Full ModReactCard corpus review synthesis (S138) — cross-cutting patterns

**Scope:** All 45 reviewable ModReactCards across Ring + 5 factions (09-16 step 2, this session). 8 cards skipped as pre-schema fossils (GHO.MOD.9/10/11, GUI.MOD.1, NET.MOD.11/12, SYN.MOD.1) — those need full re-authoring, not analysis. This entry looks across the ~45 reviewed cards for patterns invisible at the single-card level.

**A. Schema drift correlates with authoring vintage, not faction.** Every legacy-syntax/invalid-enum finding this session (items 12, 13, 15, 16, 17) came from S106–S110-vintage cards (GHO.MOD.1, NET.MOD.1/2, GUI.MOD.1) — the same generation that also used the abbreviated 8–14-row checklist and prose Status instead of the current 4-block format. The corpus has at least 3 distinct authoring eras (pre-S127 vocabulary, S128 stub-batch, S130+ mature), and the earliest era was never swept forward when the confirmed vocabulary locked in. Recommend a dedicated normalization pass over pre-S127 cards specifically, rather than catching them one at a time as review happens to reach them.

**B. `faction=Any` self-fire ambiguity is the single most repeated finding this session — ~9 cards, 3 factions** (DIR.MOD.1/3/9, GHO.MOD.2/6/7/8, NET.MOD.6/10). Telling detail: NET.MOD.2 already writes `except=Network` explicitly on its own `faction=Any` trigger — proof at least one author recognized the problem and hand-patched it locally, but the pattern was never generalized into confirmed vocabulary. Candidate direction: define `Any`'s default semantics in §6.3 (does it include the reacting card's own faction or not?), rather than requiring every card author to remember to exclude self manually.

**C. Cross-resource cost-holding is the second most repeated finding — ~10 cards, 4 factions** (GHO.MOD.5/6/7/8, GUI.MOD.6, NET.MOD.2/9/13, SYN.MOD.5/8/11). Many of these require a faction to spend a resource type it doesn't natively generate (Ghost spending Capital/Capacity, Network spending Mandate/Findings, Syndicate spending Exposure, etc.). This is a design-economy question, not a schema bug: is there a robust enough trade/conversion system that these costs are realistically payable, or are a meaningful fraction of ModReactCards effectively dead cards in practice because the faction never stockpiles the required resource? Worth a dedicated balance pass once counted precisely.

**D. String-literal `success` fields cluster on one specific unsupported effect shape, not on author carelessness.** DIR.MOD.6, NET.MOD.13, SYN.MOD.6, SYN.MOD.8 — 4 cards, 3 factions, all independently reached for prose instead of a real MutationExpr. All 4 share the same underlying shape: an effect whose actual resolution is contingent on a *separate, later* event (a standing condition that resolves when some other action concludes). This is strong evidence for item A's diagnosis (no confirmed MutationExpr vocabulary) rather than 4 unrelated authoring lapses — nothing in the current schema cleanly expresses "do X now, but the real effect depends on how Y resolves later," so every author who needed that shape independently fell back to prose.

**E. Firing-window overlaps are common and completely ungoverned.** Confirmed real overlaps this session: DIR.MOD.1/2/3 (enforcement family, same trigger event), GUI.MOD.6/7/8 (three-way overlap on Guild's own structure removal), NET.MOD.4/5 (broadcast family). No Art 03 procedure anywhere addresses what happens when a faction holds 2+ ModReactCards with overlapping or identical triggers and the triggering event fires once — sequential in submission order? Simultaneous? Player's choice? This is a missing procedural rule, not a per-card issue, and it recurred on every multi-card family reviewed.

**F. Stack behavior (2+ copies of the same ModReactCard) was flagged ⚠ on essentially every one of the ~45 cards reviewed.** This isn't 45 separate gaps — it's one gap, restated 45 times: there is no governing rule anywhere for whether holding multiple copies of a ModReactCard multiplies its effect when the shared trigger fires. Recommend resolving this once, as a single Art 03/§6 rule, rather than continuing to log it card-by-card.

**G. Positive finding — the S137 taxonomy sweep holds up completely.** Across all ~45 cards re-verified this session, zero Layer/Function/Subject mis-assignments were found; every "Taxonomy fit" row confirmed rather than corrected the prior sweep. Same for the core confirmed TriggerExpr vocabulary (`presence_chip.*`, `structure_block.*`, `standing_marker.*`, `accord.*`, `broadcast_card.placed`, `public_act.placed_on_frg`) — used correctly on the large majority of modern-era cards. The schema itself isn't broken; the gaps cluster in specific, identifiable pockets (B–F above) rather than being spread evenly across the corpus.

**Status:** Presented as a cross-cutting menu, same treatment as item 4's S137 synthesis. **B closed** — item #5 (`faction=Any` self-inclusion) resolved this exact question at S147, not reopened here. **G is a positive finding**, no action needed — confirmed still true, no further check required.

**C closed (S150).** Andy's read: this isn't a schema bug or an open design question — it was a documentation gap. Two acquisition paths already exist and are fully defined: (a) territorial expansion — base district income (Art 03 §7.4/§20, Art 01 §6.4) is paid in the *district's* own Resource Type, not the collecting faction's Native Resource, so factions naturally accumulate foreign resource types simply by holding foreign-native districts; (b) inter-faction trade at Debrief (§11.0, "any terms," unrestricted). The Translation (§19.1) is the fallback for when neither applies, not the primary mechanism — Art 03 §7.4/§20 never stated the resource-type point explicitly, which is what made the ~10 cross-resource ModReactCards look like a possible balance problem. Fixed at the source: §7.4 restructured (§7.4.0 Calculate District Income → §7.4.0.0 Apply Affinity Bonus / §7.4.0.1 Collect District Income; §7.4.1 Calculate Structure Block Income → §7.4.1.0 Declare / §7.4.1.1 Collect; §7.4.2 Collect Passive Generation) plus a new §20 "Resource Type" note stating the rule directly and naming both acquisition paths and Translation's fallback role. Also fixed the opening §7.4 paragraph, which read "each Faction Player... collects their own resources" — worded ambiguously enough to imply faction-native-only and to blur that the draw from the Reservoir is a player action, not an ARBITER one. Closes PM05 03-n04 (same underlying reference gap, flagged S84, never resolved). The 10 originally-flagged cards (GHO.MOD.5/6/7/8, GUI.MOD.6, NET.MOD.2/9/13, SYN.MOD.5/8/11) were not touched — their costs are legitimate under the now-documented rule, not defects.

**Side finding, flagged not fixed:** pulling those 10 cards' actual `cost` fields surfaced GHO.MOD.6/8 and GUI.MOD.6 using a `Resource(...)` wrapper-function cost notation (e.g. `Resource(faction(trigger.faction).native, 1)`, `Resource(Capacity, 1)`) that doesn't match the bare `ResourceType * n` / `faction.X.native * n` CostExpr grammar confirmed in §6.3 (item #22, S148) — a fourth cost-notation style, undetected until this pass. GHO.MOD.6/8 also use a *dynamic* native reference (`faction(trigger.faction).native`, the triggering faction's native type, not Ghost's own) — a materially different, harder-to-plan-for cost shape than the other 8 cards' fixed cross-resource costs, since the required type varies by who triggers the card. Not resolved here — out of scope for #20-C, candidate for its own schema_cleanup_log item.

**E/F closed (S150).** Full corpus re-check first (all 93 ModReactCards, not just the ~45-card S138 faction sample): found several more genuine overlap families beyond the 3 originally named (DIR.MOD.1/2/3, GUI.MOD.6/7/8, NET.MOD.4/5) — GUI.MOD.2/3/4, GHO.MOD.2/3/4, NET.MOD.6/10, STD.MOD.99/104 (Ring 1, two unrelated cards sharing an identical trigger), DIR.MOD.5/8 (conditional overlap). Turned up a bigger positive finding, though: Art 03 §18 (React Card Rules) already substantially governed this — §18.0.1/§18.1/§18.1.0 already established "first to announce pauses play," "only one React resolves at a time," and "a second React may only fire against the new board state produced after the first resolves." The S138 review's "no Art 03 procedure addresses this" read missed that §18 existed. Andy's ruling filled the two actual gaps: (1) §18.1.0's tiebreaker was vague ("ARBITER decides") — specified as initiative order; (2) §18.1 never addressed a single faction holding 2+ eligible cards (or 2+ copies of the same card) for one event — now explicit: that faction chooses which single card to present, the others wait for an independent future trigger. Andy's own framing, direct: the resolving card's effect changes the board state (per existing 7.2), so the "duplicate" trigger is exhausted, not skipped — this single mechanism answers both E (cross-card overlap) and F (same-card stacking) at once, since a card held in 2+ copies is just another case of "2+ eligible cards, same faction, same event." New Art 00a corollary **§7.2c (Triggering Conditions Are Exhausted on Resolution)** written to state this at the Governing Rules level — generalized to any reactive mechanic, not phrased around ModReactCard specifically (Andy's correction: the first draft was too specialized for a GR-level rule); ModReactCard's specific announce/tiebreak/choice-among-own-cards procedure stays entirely in Art 03 §18 as one application of the general rule. F's original framing (does N copies multiply the effect N×) is answered "no" as a corollary, not because deck copy-counts are decided (still open, PM05 04-n136) — the rule holds regardless of how many copies a future balance pass allows. No card content changed; this is a procedure/GR fix only.

**A closed (S150).** Re-inspected all 4 named cards (GHO.MOD.1, NET.MOD.1, NET.MOD.2, GUI.MOD.1) directly. The item's actual complaint — abbreviated 8–14-row checklist and prose Status instead of the modern 4-block format — is already fully resolved on all 4; each carries the complete ~22-row checklist and proper Design Rationale/Card Story/Checklist/Status structure, apparently picked up incidentally during the earlier items 12/13/15/16/17 content fixes without ever being logged as closing this item specifically. No structural work needed.

Re-inspection did surface real content the earlier passes missed, fixed here: **(1)** 3 instances of a fourth, previously-uncaught presence-mutation call shape — `faction(acting).presence_chips(district(X)).add(1)` — that survived item #4-A's corpus-wide sweep: GUI.MOD.1, NET.MOD.1, and (notably, not one of the 4 vintage cards) NET.CA.7. All converted to the canonical `arbiter.place(presence_chip, district=X, faction=acting, count=1)`. **(2)** Three stale checklist notes corrected: GUI.MOD.1's "Trigger validity" row claimed `presence_chip.removed(faction=X, district=Y)` wasn't yet confirmed — it is (PM05 04-n195 item 1); the row was conflating it with the separate, still-open `ring=` parameter gap (item #3), which this card doesn't use. NET.MOD.2's "Outcome determinacy" row carried a stale "(once the expression syntax is eventually corrected)" note for a fix already done (item #17). GHO.MOD.1 had a duplicate stray "Outstanding Issues" bullet list (obsolete content — referenced a card name/ID that were already finalized) sitting above its proper `#### Outstanding Issues` section — removed. Fence count (776) and `Card()` count (386) unchanged; monolith regenerated.

**Open question surfaced, not resolved:** GUI.MOD.1 and GUI.MOD.10 both write their trigger's district parameter as `district=district(trigger.target)` — referencing `trigger.target` inside the definition of `trigger` itself, which reads as circular (the trigger event hasn't matched anything yet at the point it's being defined). Two independent cards share the exact same pattern, so it's not a one-off typo. Needs Andy's read: is `trigger.target` a valid generic wildcard-binding construct meaning "match any district, bind it for downstream reference" (in which case this needs to be added to confirmed §6.3 vocabulary), or is it an authoring bug that should be a bare `district=Any` with downstream fields using `trigger.district` instead (the pattern every other confirmed trigger in the corpus uses)?

**D closed (S150).** The 4 named cards turned out to split into three distinct situations, not one shape as originally framed:

- **SYN.MOD.8** wasn't this pattern at all — its `success` was a semicolon-joined string of two already-valid `arbiter.place()` calls, never converted to a real multi-mutation expression. Converted to `list([...])`, matching the confirmed GHO.MOD.7/NET.MOD.10 precedent.
- **DIR.MOD.6** didn't need new vocabulary — `persistence_effect = game.board_condition(scope=, effect=on(TriggerExpr): MutationExpr)` was already confirmed and already used in 5 other cards; DIR.MOD.6 simply never adopted it, stuffing the same ongoing-condition effect into `success` as prose instead. Rewritten to use it directly.
- **NET.MOD.13 and SYN.MOD.6** were the genuine gap: a bounded effect tied to one specific target object (the triggering PA), clearing when that object resolves — a shape the schema had no vocabulary for. Closed by reusing `persistence_condition`/`persistence_effect` as designed rather than inventing a new field: `persistence_condition = not trigger.card.resolved` keeps a Standing Condition active only while its bound card is still pending, clearing automatically the moment it resolves (NET.MOD.13 reclassified Immediate→Seasonal to unlock this — Seasonal's Quarter-end clear is just the backstop, `persistence_condition` does the real work). New confirmed §6.3 vocabulary: `public_act.resolved(pa=X)` as a valid TriggerExpr (already used unconfirmed elsewhere — formalized, not invented); `trigger.card.resolved`/`trigger.card.outcome` accessors for a specific referenced card's state and result; `arbiter.protect(target, from=)` for temporary immunity; `on(TriggerExpr): MutationExpr` as a standalone event-bound MutationExpr form, usable directly in `persistence_effect` and branchable via the same `BoolExpr: MutationExpr` colon syntax already confirmed for `affinity`'s ConditionalExpr. SYN.MOD.6's `persistence_clearing_trigger` also converted from prose to real TriggerExpr in the same pass, closing the PM02 L300 gap that had been deliberately deferred for this exact card.

**Side finding, flagged not fixed:** fixing SYN.MOD.8 surfaced a second, undocumented multi-mutation notation style — bare tuple `(A, B)` — coexisting with the confirmed `list([A, B])` form (GHO.MOD.7/NET.MOD.10) elsewhere in the corpus (e.g. NET.CA.7's `successcrit`). Not resolved here — candidate for its own schema_cleanup_log item.

Item #20 (all lettered sub-items) is now fully closed.

---

### 21. Category: `ps_shift(faction="target", ...)` ModActionCard has no restriction enforcing the host actually has a target — CLOSED, not a gap

**Surfaced by:** STD.MOD.34 Word to the Wise (S139, Ring 1 Portable ModActionCard review — first target-hinder `ps_shift` card reached). Originally logged as: `ps_shift(faction="target", delta=-1)` resolves against whichever faction the host CA/PA declares as `target_faction` (§6.1), and nothing enforces that the host has one — as specced, a faction could attach this card to a targetless host at Dispatch, leaving ARBITER nothing to shift against.

**Closed (Andy, S139):** Not a gap. A ModActionCard does not declare its own target — its target IS the host action card it's submitted with (Art 03 §9.1.1/§9.4.0.1 packet-pairing at Dispatch). It enhances the effect of the card it attaches to; `faction="target"` is definitionally the host's target, not a separately-validated field ModActionCard needs a `restriction` clause to police. A player simply wouldn't pair a target-hinder `ps_shift` card with a targetless host — same non-issue as pairing `cost_reduction` (PA-only) with a CA host. No schema fix needed; no restriction field, no Art 03 rule.

**Retroactive note (corrected):** DIR.MOD.20 Public Reprimand's `arbiter_note` (S135 stub-pass content) describes `faction="target"` resolving to whichever faction the host names — that description is correct as far as it goes; it does not describe an actual gap, and the S139 review's reading of it as evidence of an "unformalized" constraint was the error, not the note itself.

**Status:** Closed S139, not a gap. Affected 22 cards had an incorrect ⚠ flag added and removed in the same pass (STD.MOD.34/35/46/47/58/59/70/71/82/83/94/95, DIR.MOD.20/24, GHO.MOD.24/25, GUI.MOD.23/24, NET.MOD.27/28, SYN.MOD.24/25) — see PM02 entry revising L267. PM05 **04-n179** closed accordingly. GUI.MOD.23/24 retain a separate, still-valid note (Guild's own CA/PA mix skews self/territory-directed, so these two cards see fewer eligible hosts than Directorate/Syndicate's equivalents — confirmed by Andy as a distinct, surviving observation, not dissolved by this correction).

---

### 22. Category: `cost` expression term missing resource-type attribute — confirmed corpus pattern in Standard CA

**Surfaced by:** STD.CA.1 Build Structure (S141, first card of the CA/PA review phase — oldest CA in the system, signed off S63). `cost = resource.faction(acting) * 1 + resource.district(native) * 1` — the first term (`resource.faction(acting)`) has no resource-type attribute, unlike most cost expressions in the corpus, which specify one (e.g. STD.CA.13: `resource.faction(acting).native * 2`). Design Rationale states the intent plainly ("dual cost: 1 faction native + 1 district native"), so the likely correction is `resource.faction(acting).native * 1` — but per [[feedback-review-pass-scope]] this is flagged, not fixed, during the review pass.

**Confirmed corpus-wide (S141, full Standard CA set now reviewed):** same missing-attribute shape on **STD.CA.2, STD.CA.3, STD.CA.4** (all `resource.faction(acting) * 1 + resource.district(native) * 1`) and **STD.CA.5** (`resource.faction(acting) * 1`, mono-resource). 5 of 16 Standard CA cards affected — all clustered at the low card-ID end (CA.1–5), while CA.6 onward (Exposure, Capital, Mandate, IntelToken-typed costs) are all correctly typed with an explicit attribute (`.exposure`, `.capital`, `.mandate`, `.native`). Suggests an early-authoring-era gap (same "vintage correlates with drift" pattern seen in the ModReactCard corpus, schema_cleanup_log item #20A) rather than a random scatter.

**Deeper gap, same shape as item A (no confirmed MutationExpr vocabulary):** there is no confirmed `CostExpr` vocabulary in Art 04 §6.3 either — grepped directly, no enumerated list of valid `resource.*` call forms exists the way §6.3 enumerates TriggerExpr forms. So there's no canonical text to check `resource.faction(acting)` (bare) against; it may be that bare = native-by-default was an intentional early shorthand, later abandoned in favor of always-explicit typing once non-native cost resources (Capital, Exposure, Mandate) entered the set. Not resolved — this session's job was to flag, not decide.

**Contrast confirmed (S142, Directorate PA review):** all 11 Directorate PA cards are correctly typed — every `cost` expression carries an explicit resource attribute (`.mandate`, `.capacity`, `.capital`, `.findings`), including the two most complex costs seen in the corpus so far (DIR.PA.6's triple cross-resource Mandate+Capital+Findings, DIR.PA.5's Mandate+district-native+Capital). This confirms the untyped-cost bug is not universal — it's specific to the Standard set (both CA and PA) plus one isolated Directorate CA instance, not a general corpus habit.

**Status:** Open, previously 6 confirmed instances (STD.CA.1–5, DIR.CA.5). **Massively extended S142 (Standard PA review): all 8 Standard PA cards show the identical untyped shape** — STD.PA.1/2/4/6/7/8 (`resource.faction(acting) * n`, mono-resource) and STD.PA.3/5 (`resource.faction(acting) * 1 + ...`, dual/cross-resource). This breaks the earlier "clusters at the low card-ID end, CA.1–5 only" read (§ above, also synthesis item #34-A's claim that Standard's only bug was "confined to CA.1–5") — the untyped form isn't a legacy-vintage artifact scoped to five early CA cards, it's the default convention across the *entire* Standard PA set regardless of card ID, alongside its partial presence in Standard CA. 14 confirmed instances total now, across Standard CA (6/16) and Standard PA (8/8) plus Directorate (1). Standard PA's 8/8 rate is notably higher than Standard CA's 6/16 — worth checking whether PA cards as a category default to this shorthand more than CA cards do, once Directorate/Ghost/Guild/Network/Syndicate PA sets are reviewed. Candidate direction, not proposed: either normalize to explicit `.native` typing (matching the majority of the corpus), or confirm bare `resource.faction(acting)` as valid native-shorthand syntax and leave as-is — Andy's call, once a normalization/fix pass is scheduled.

**Related, broader gap confirmed (S141, Syndicate CA review):** beyond the missing-`.native`-attribute bug, the Syndicate Accord-manipulation cluster (SYN.CA.10/11/12, all S111–S130 vintage) uses a **third distinct cost-notation style** entirely: bare resource-type-as-callable (`Capital(3)`, `Capital(2)`, `Capital(1) + Mandate(1)`) with no `resource.faction()` wrapper at all — no explicit faction binding, unlike every other notation style in the corpus. **4th confirmed instance (S142, Syndicate PA review): SYN.PA.3's `cost = Capital(1)`** — same bare-callable style, same faction, now confirmed in both CA and PA phases for Syndicate specifically. Combined with the already-noted absence of a confirmed `CostExpr` vocabulary in Art 04 §6.3 (no enumerated valid forms the way TriggerExpr has one), the corpus now has at least three different cost-expression conventions coexisting: (1) `resource.faction(X).type * n` (majority), (2) bare `IntelToken(...) * 1` (item #10), (3) bare `Type(n)` (this note, now 4 instances, all Syndicate). Candidate direction, not proposed: confirm one canonical form in §6.3 and sweep the others — Andy's call.

**Status: Closed S148.** Picked back up cold — the S147 decision session was interrupted mid-message and nothing had been written down, so this session re-derived current corpus state fresh rather than trusting the carried counts (same "re-derive, don't trust a carried summary" discipline as 04-n188/L305). Fresh full-corpus grep for the untyped bare form found **15 instances, matching the carried count but with a different card list**: STD.CA.1–5 (5) + **STD.PA.1–9, all 9 PA cards** (not PA.1–8 as previously logged — STD.PA.9 Floor Act, added S143 after the original count, carries the identical untyped shape) + DIR.CA.5 (1, both its `cost` and `boost` fields) = 15 total. Fresh grep for the bare-callable style found **5 instances, one more than the logged 4**: SYN.CA.10/11/12, SYN.PA.3 (previously logged) + **SYN.MOD.11** (not previously flagged in this log — its checklist already read ✓ and never called out the notation). Andy's three calls: **(1)** the 15 untyped instances normalized to explicit `.native` — all 15 draw the acting faction's own native resource, confirmed per-card before editing, no ambiguous cases. **(2)** the 5 bare-callable instances normalized to the majority `resource.faction(acting).type * n` wrapper form — `Capital(3)`→`resource.faction(acting).capital * 3`, `Capital(1) + Mandate(1)`→`resource.faction(acting).capital * 1 + resource.faction(acting).mandate * 1`, etc. — no longer a second valid style. **(3)** a confirmed `CostExpr` vocabulary written into §6.3 (`Part1_Core.md`), same treatment as TriggerExpr/MutationExpr: `resource.faction(X).type * n` (X = acting/target/target1/target2/target_faction/named Faction; type = native/capital/mandate/exposure/findings/capacity, always explicit), `resource.district(Y) * n`, plus the existing IntelToken forms (schema_cleanup_log #10) folded in as the second confirmed category. Checklist rows updated per-card — only the notation-specific flag content changed; rows carrying other, unrelated open gaps (DIR.CA.5's missing `card_id`/`ps_framing`, SYN.CA.10/11/12's missing `boost`/`ps_framing`, SYN.CA.11's unqualified `standing += 1`) correctly remain ⚠ for those separate reasons, not blanket-flipped. Monolith regenerated; fence (776)/`Card()` counts unchanged. No corpus content changed beyond notation. Full detail: PM02 L307.

**Addendum, caught by Andy same session (S148):** the `.native`-typing fix above wasn't the whole notation bug — `resource.district(native)` (8 corpus instances: STD.CA.1–4/10, STD.PA.3, DIR.CA.5 ×2) has the identical defect one level down, not just a missing attribute. Checked against Art 01 §6.4: no district generates a resource type literally called "Native" — each of the 21 districts has one fixed, named Resource Type (Mandate, Capital, Findings, Capacity, or Exposure). "native" was being used as the *district-reference argument itself* (in the slot that should identify *which* district — `target`/`target_district`/`each_target`, per the already-correct instances elsewhere in the corpus, e.g. Guild's `resource.district(target_district).native`), not as a resource-type attribute at all — the same untyped-argument shape as item #22's main fix, just on the district side of `resource.*` instead of the faction side. All 8 instances corrected to name the actual district reference already used elsewhere in the same card's own `restriction`/`success` fields (`target` for STD.CA.1–4/10 and STD.PA.3, `target_district` for DIR.CA.5) with `.native` appended, matching the already-established pattern. Also caught and fixed in the same pass: **NET.PA.4** used a third, entirely separate cost-notation form, bare `district_native(target_district)` (no `resource.district()` wrapper at all) — normalized to the same canonical `resource.district(target_district).native`. §6.3's `CostExpr` entry (written this session, addendum above) corrected: `resource.district(Y)` → `resource.district(Y).native` (the `.native` suffix is not optional — it's the same relative "look up this referent's own generated resource" meaning as `faction(X).native`, just resolved against a district instead of a faction). One further finding, **flagged not fixed** (a content question, not notation): **GUI.PA.1 Civic Works Mandate**'s `affinity` field reads `faction(Guild): cost.resource.district(native) = 0  # waived for both districts`, and its own Balance checklist row states "both district natives waived (Guild)" — but the card's actual `cost` field (`resource.faction(Guild).capacity * 2 + resource.faction(Guild).capital * 1 + resource.faction(Guild).mandate * 1`) contains no `resource.district(...)` term at all for the affinity to zero out. Either the cost once had a district-native component that was removed without updating `affinity`/Balance, or the affinity/Balance language is simply stale — a content call, not resolved here (see item #46). Checklist rows updated for all 8 typing fixes + NET.PA.4; monolith regenerated, fence (776)/`Card()` counts unchanged. Full detail: PM02 L308.

---

### 25. Category: `function = Move` used as a Function value — not in the confirmed Function Vocabulary

**Surfaced by:** DIR.CA.2 Detain and DIR.CA.4 Tactical Redirection (S141, Directorate CA re-derivation). Both declare `function = Move`. Checked directly against `ref_taxonomy.md`'s Function Vocabulary table (§5.1): confirmed Function values are Add / Remove / Redirect / Modify / Protect / Block / Copy / Reveal / Shift / Corrupt — **Move is not among them.** "Move" is one of the 7 Physical Verb Primitives (Art 02 §13.1) — the underlying mechanical action, not a Function. Two Functions map to Move as their primitive: **Redirect** ("changes ownership, destination, or allegiance") and **Shift** (track values only, doesn't apply to physical tokens). Redirect looks like the correct Function for both cards' actual behavior (both relocate a component to a new owner/destination).

**Not a fresh authoring slip:** DIR.CA.2's own checklist states this was a deliberate correction — "function corrected S107 L226 (Remove → Move; success block uses game.move(), not game.remove())." Someone actively changed a valid Function (Remove) to an invalid one (Move) at S107, apparently reasoning from the physical-verb layer rather than the Function-vocabulary layer. `v_card_mechanical_alignment` (DB) shows both cards as `Abstract Function (No Mechanical Verb)` — consistent with Move having no `function_verb` entry, but this reads differently from the documented-safe "Abstract Function" pattern (Modify/Block/Protect meta-constraints with no primitive by design) — Move has a clear, defined primitive, it's just not a valid Function-layer term.

**Third instance confirmed directly (S141, Network CA review):** `04___Card_System___Part4d_Network.md:593` is **NET.CA.8 Fake News** — same `function = Move` on `subject = DeploymentMarker`, same `Abstract Function` DB flag. This was anticipated from the earlier Directorate-pass grep and is now confirmed by direct card review rather than just a corpus grep hit.

**Status: Closed (Phase 5 / 04-n196, S150).** Normalized `function = Move` → `function = Redirect` on all three (DIR.CA.2, DIR.CA.4, NET.CA.8 Fake News), matching the documented Function Vocabulary.

**Related, not identical (S141, Guild CA review):** GUI.CA.4 Construction Crew used `function = RemoveRestriction` — also not in the Function Vocabulary, also DB-flagged Abstract Function. Different invalid value than `Move`, same underlying category (a card author reaching for a descriptive verb outside the confirmed 10-value vocabulary). Normalized S150 to `function = Modify` ("alters cost, value, or attribute without changing fundamental state").

**5th instance found during S150 execution, not caught by the original sweep:** GUI.PA.5 Zoning Exemption also carried `function = RemoveRestriction` — same fix applied (→ `Modify`). Total: 5 confirmed invalid-Function instances across 2 invented values (Move ×3, RemoveRestriction ×2), all normalized. `card_status` DB synced for all 5. Corpus-wide grep re-run post-fix confirms zero remaining `function = Move`/`RemoveRestriction` instances.

---

### 26. Category: `game.active_permanents(faction=, ring=)` — new ARBITER-facing behavior not established as a general procedure

**Surfaced by:** DIR.CA.6 Institutional Audit and DIR.CA.7 Institutional Brief (S141, Directorate CA re-derivation). Both cards' yield scales with "count of active Directorate Permanent cards in the same ring as the target district" — both checklists claim `Supported by game procedure ✓ ... existing permanent card procedure`. Grepped all 8 Art 04 Part files and the Whiteboard reference set for `active_permanents` / any "count Permanents by ring" language: **it appears nowhere outside these two cards.** No Art 03/07 procedure defines this counting mechanism generally.

**Why this matters:** Governing Rule 6.1 ("ARBITER executes general procedures, not card-specific instructions... New ARBITER behavior must be defined as a generalizable procedure in Art 03 or Art 07 before the card is finalized") and Design Pillar 4.7b (ARBITER Cognitive Efficiency) both bear directly on this. The cards' own "existing procedure" claim doesn't hold up against a direct check — it's new, card-specific ARBITER-facing logic that hasn't been formalized as a general procedure elsewhere.

**Status: Closed (S150).** Andy's ruling: formalize it, with an explicit scope clarification — this counts currently-active PublicAct/ModReactCard cards creating a permanent standing effect (persistence=Permanent, card-as-condition), not every card with persistence=Permanent regardless of type (a CovertOperation's one-time Permanent board effect, e.g. a token placement, doesn't qualify). Added to Part1_Core.md §6.3's confirmed MutationExpr helper list, same format as `arbiter.remove()`/`arbiter.modify()`. All 3 confirmed instances (DIR.CA.6 Institutional Audit, DIR.CA.7 Institutional Brief, DIR.PA.9 Charter Grant) updated: checklist "Supported by game procedure" rows ⚠→✓, DIR.CA.6's Outstanding Issues item removed (now resolved by the formal entry, not carried forward). No card content changed — the mechanism itself was already consistent across all 3; only the missing formalization was the gap. Fence count (776) and `Card()` count (386) unchanged; monolith regenerated.

---

### 27. Category: DIR.CA.8's subject `Difficulty` is an unregistered, non-component Subject term

**Surfaced by:** DIR.CA.8 Enhanced Scrutiny (S141, Directorate CA re-derivation). Taxonomy is `Resolution / Modify / Difficulty`. `v_card_mechanical_alignment` (DB) flags this card as `Non-component Subject` — distinct from the `Abstract Function` flag its sibling Modify/Block/Protect cards in this set correctly carry (which `ref_taxonomy.md`'s own gap-pattern table documents as an expected non-issue). "Difficulty" doesn't appear in `ref_taxonomy.md`'s Subject Vocabulary table at all, and per that same table's gap-pattern guidance, a `Non-component Subject` result means the string is missing from `card_subject_map` — a registration gap, not a known/expected pattern like the Abstract Function cases.

**Status:** Closed (PM05 04-n195, Phase 4 item 7, S149). Andy's rule: Subject values must be valid Art 04b taxonomy Subjects, not just registered ad hoc — "Difficulty" wasn't a real component/Subject at all (not in ref_taxonomy.md, not a physical component), so it doesn't pass; the 2 cards using it were reclassified rather than the string registered. **DIR.CA.8** — design_note names the actual mechanism (existing Modifier tokens, Art 02 §11, DB:47) — reclassified Subject=Difficulty → `ModifierToken`. **GUI.CA.9** — suppresses a named CA's dice roll entirely, modifying a Covert Operation's resolution — reclassified Subject=Difficulty → `CovertOperation` (already-valid vocabulary, no new registration needed). **GHO.CA.15** — `TargetProfile` checked against ref_taxonomy.md's Corrupt-target list: already real, valid vocabulary (DB:48 component) — registered in `card_subject_map` as-is, no reclassification. `card_subject_map`'s dangling `Difficulty` row (component_id=NULL) removed; `ModifierToken`/`TargetProfile` added (component_id 47/48); `card_status` DB rows synced for all 3 cards.

---

### 28. Category: variable-cost mechanic implemented with a bare undeclared variable instead of the schema's `boost` field

**Surfaced by:** GHO.CA.8 Full Take (S141, Ghost CA review). Cost/success/successcrit all scale with a bare `n` variable declared at submission ("Ghost declares n at submission"), with no `boost=` field anywhere in the spec. This is exactly what the schema's `boost: BoostExpr` field exists for (`design_reference_card_system.md` §6 Field Groups: "player submits additional resources beyond base cost; ARBITER detects at Beat 0; success fires (1+n) times") — and DIR.CA.5 Sanctioned Raid (reviewed same session) correctly uses `boost = True: resource.faction(acting)...` for the identical shape. GHO.CA.8 reimplements the same mechanic ad hoc instead of using the confirmed field.

**Status:** Closed (PM05 04-n195, Phase 4 item 8, S149). Normalized to `boost = True: Findings * 1`, matching DIR.CA.5's pattern exactly (base cost + boost unit, success/successcrit magnitudes rewritten in terms of `(1 + n_boost)`, no threshold scaling — preserves the original card's math 1:1).

---

### 29. Category: bare string-literal `success` fields on undesigned CA stubs — same fossil shape as the already-closed 04-n174 sweep, new instances

**Surfaced by:** GHO.CA.13 Phantom Accounts, GHO.CA.14 Ghost Protocol, GHO.CA.15 Routing Override (all S141, Ghost CA review, all marked `*(stub)*`). Each card's `success` field is a bare prose string ("Arbiter places 1 DA-02...", "The Arbiter invalidates and removes...", "Ghost corrupts the first CA...") rather than a MutationExpr — the same category of defect 04-n174 closed S140, but that sweep covered a specific list of ModReactCard fossils (GHO.MOD.9/10/11, GUI.MOD.1, NET.MOD.11/12, SYN.MOD.1) and didn't touch these three CA stubs. All three are also missing nearly the entire base-Card field set (`card_id`/`doctrine_mod`/`boost`/`ps_framing`/`persistence`/`portrait`/`perspectives`) — sparser than even the oldest Standard/Directorate stub cards.

**Status: Closed (Phase 5 / 04-n196, S150).** All 13 instances fully rewritten — the original "12 confirmed" count undercounted by one (its own prose already listed 6 GUI.PA cards, which totals 13 with the other 7; GUI.PA.5 was caught and completed alongside the rest once the discrepancy was noticed).

**GHO.CA.13/14/15, GUI.CA.7/8** — full spec: real structured `success`/`restriction` (MutationExpr/BoolExpr, no more bare strings), all base fields populated, Design Rationale/Card Story/checklist/3-voice perspectives written. GHO.CA.15 (pattern-setter) and GHO.CA.13 each introduce new, clearly-flagged §6.3 primitives (`resolution_grid.first_op()`, `arbiter.corrupt()`, `game.resource_generation()`) grounded in already-confirmed concepts (Resolution Grid, submission-order tiebreaking, Upkeep Step 5's income formula) rather than invented from nothing — GHO.CA.14 reuses GHO.CA.15's primitive rather than adding a new one. GHO.CA.13's DA-02 PhantomRecord component (Art 04 §12a.3) had its own "pending GHO.CA.13's design pass" flag closed as part of this — `generation_snapshot`'s formula is now concretely defined.

**DIR.PA.7 Curfew, DIR.PA.8 Subpoena** — DIR.PA.7's persistence model was already resolved under #44; this pass converted its `persistence_effect` from prose to a structured `game.board_condition()` reusing the Deployment Marker's existing Blocked-face mechanic (new primitives: `placement_phase.completed()`, `deployment_marker.placed(district=)`). DIR.PA.8 converted from an inconsistent `d100`+prose-choice hybrid to the established `ElectPlayer` pattern (Automatic, `on_accept`/`on_decline`, matching SYN.PA.1/SYN.PA.3) — a deliberate mechanical change from the stub's original dice-gated framing, flagged in its own checklist; also retaxonomized `Capital`→`NativeResource` subject (target pays in their own currency, not Capital specifically), confirmed against `ref_taxonomy.md`'s own `Economy|Remove|NativeResource` precedent.

**GUI.PA.3/4/5/6/7/8** — all 6 fully rewritten. GUI.PA.3 (Heritage Registry) and GUI.PA.8 (Structural Subsidy) both needed the Card-as-Condition pattern; GUI.PA.8's `persistence` was changed from the stub's `Permanent` to `Seasonal` (an indefinite unclearable condition would be a real corpus outlier — every other Permanent card-as-condition has a clearing mechanism). GUI.PA.5 had the identical "for the next Quarter" ambiguity #44 resolved elsewhere on DIR.PA.7/SYN.PA.5 — corrected to "remainder of the Quarter," matching Seasonal's actual definition; its `success` now uses `game.world_condition()`, matching SYN.PA.2's confirmed Seasonal-timed-effect pattern. GUI.PA.6's `restriction`/`success` bare strings (including the English `AND`→Python `and` fix) converted to real expressions reusing SYN.CA.3's confirmed `game.transfer()` pattern. GUI.PA.4's already-valid-Python-in-quotes `success` string simply had its quotes removed and executed as real code, same treatment as GUI.CA.7.

All 13 cards: full Design Rationale, Card Story, and 3-voice `perspectives` (acting + doctrinally-aligned + doctrinally-opposed) added per `ref_card_types.md`'s FactionSpecific convention, none using cross-card references per the L276 rule. `outcome_type` filled on all — this also closes the remaining 8 instances of #38 that were folded into this item. Every new/extended §6.3 primitive introduced along the way is flagged in its card's own checklist and Outstanding Issues, not silently assumed confirmed — none of this required a P19/GR 6.1 rule exception. Monolith regenerated after each card; fence counts verified unchanged throughout.

---

### 30. Category: `card_status` DB shows NULL taxonomy for a card whose actual spec has valid, correct taxonomy

**Surfaced by:** GUI.CA.10 Development Order (S141, Guild CA review). The card's own code correctly declares `layer=Territory, function=Add, subject=StructureBlock` — a valid, Legalized-shape taxonomy assignment matching its own checklist claim. But `card_status` (DB) shows `layer`/`function`/`subject` all `NULL` for this card, and `v_card_mechanical_alignment` reports `Abstract / No Subject` as a result — a false negative caused by DB/MD drift, not a card content problem. Per `feedback_card_status_sync.md`, `card_status` should stay in sync with Art 04 "immediately... never deferred" — this is a case where that didn't happen (or the row was created before the card's taxonomy fields were finalized and never back-filled).

**Status: Closed S148 (Phase 2 / 04-n193, PM02 L322).** Full corpus reconciliation run (not just the 2 named instances) after a DB infrastructure incident forced a full restore (PM02 L321) — genuine DB-stale taxonomy confirmed on exactly 3 cards: GUI.CA.10 and GUI.PA.10 (both named here) plus **SYN.MOD.1** (item #42's list, same shape). All 3 synced `layer`/`function`/`subject` from the `.md` spec into `card_status`. Four other candidates surfaced during the same reconciliation (GUI.MOD.9, SYN.CA.11, SYN.CA.12, SYN.PA.3) turned out to be false positives from a parser bug in the reconciliation script, not real drift — verified by hand against both `.md` and DB directly; both sides already agreed. Not a Guild-specific pattern after all — 2 of the 3 real instances are Guild, 1 is Syndicate.

---

### 31. Category: cost value in code doesn't match cost stated in prose — genuine content mismatch, not a stale-annotation issue

**Surfaced by:** NET.CA.4 Network Cascade (S141, Network CA review). Both the Design Rationale ("pay NET.CA.4's Exposure×2 to extend that disruption") and the Balance checklist row ("Exposure×2 for adjacency extension") independently state the cost is Exposure×2. The actual code: `cost = resource.faction(acting).exposure * 1 + resource.faction(acting).findings * 1` — Exposure×1 + Findings×1. This is different from the already-tracked stale-checklist-prose pattern (DIR.CA.7/NET.CA.7's "PublicStanding" mentions, where the code was correct and only the label was outdated) — here, *two independent prose locations agree with each other* and *both disagree with the code*, so it's not obviously a copy-paste-forward error from one stale mention. Genuinely unclear which is right: was the design intent Exposure×2 and the code was miswritten, or was the design later changed to cross-resource (Exposure+Findings) and the prose never updated?

**Status: Closed (Phase 5 / 04-n196, S150).** **NET.CA.4** was already resolved at PM02 L282 (S144) — code confirmed correct, prose fixed; only a stale "Exposure×2" checklist row remained (Doctrine alignment), now corrected. **SYN.CA.9** — code had already been deliberately repriced twice since the prose was written (most recently Capital×2+Mandate×1 at PM02 L278, the S144 UVM pass); prose updated to match, no new ruling needed. **SYN.CA.3 and SYN.CA.5 — Andy's ruling (S150): code wins on both.** SYN.CA.3's cross-resource cost (Capital×3+Findings×1+Exposure×1) and SYN.CA.5's (Capital×2+Exposure×1) are the real design; Design Rationale/design_note/checklist rows rewritten to describe the actual cross-resource cost rather than the old single-resource "headline" framing. All 4 cards' Design Rationale, Balance, and Resource cost positioning rows now describe their real costs with no prose/code gap. Confirms the synthesis read (item B below): this was a systematic habit of writing the headline cost before cross-resource balancing, code consistently being the authoritative final value.

---

### 33. Category: `Resource cost positioning` checklist row filled with the raw guidance template text instead of an actual assessment

**Surfaced by:** SYN.CA.10 Accord Transfer and SYN.CA.11 Redline (both S141, Syndicate CA review). Both cards already had a "Resource cost positioning" row (unlike most of the corpus, which was still missing it entirely as of this review) — but the row's content was the literal guidance question copied verbatim from `design_reference_card_system.md`/Art 04 §5 ("Is this card's cost mono-resource... *(P28)*"), with no Pass-column value and no actual per-card assessment. Functionally equivalent to the row not existing, but structurally different — a checklist audit that only checks "is the row present" would have missed this, where checking "does the row have real content" catches it. Scaffolded with real assessments in this pass (mono-resource for both, per their actual `Capital(n)` cost fields).

**Status:** Fixed as scaffolding during this pass (not a design decision, just filling in the missing assessment) — noted here because it's a distinct failure mode worth watching for elsewhere: a checklist row can exist and still be unfilled. Ran the suggested grep (`grep -rln "Is this card's cost mono-resource" 04___Card_System___Part*.md`) — one more hit confirmed: **GHO.PA.5 Agency Recruitment Fair**, in the Ghost Public Acts section (out of scope for this CA-only pass, not touched — flagged here so it isn't missed when PA review starts).

**GHO.PA.5 fixed S142 (Ghost PA review):** same treatment — real assessment written in (mono-resource, Findings × 1, correctly typed).

**SYN.PA.3 Data Acquisition fixed S142 (Syndicate PA review):** same treatment — real assessment written in (mono-resource, Capital × 1, floor-power). 4th confirmed instance total, all now scaffolded with real content across both CA and PA phases. All 4 instances now confirmed within/adjacent to the Syndicate faction (SYN.CA.10/11 + SYN.PA.3) plus GHO.PA.5 — worth checking whether the raw-template-text failure mode correlates with a specific authoring batch/session, same as other Syndicate-clustered findings this review (#7, #22's third notation).

---

### 23. Category: Portrait penalty for a faction acting against its own doctrine — coverage may be inconsistent corpus-wide

**Surfaced by:** STD.CA.1–16 re-derivation pass (S141), via the Guild "cannot operate covertly" doctrine question (§5b above, `ca_pa_review_notes.md`). Andy's framing: a faction submitting a card that runs against its own stated doctrine should generally carry `submitter=-1` on that card's portrait entry — this is already the pattern on some cards (STD.CA.2/4 give Guild `-1` for demolition/undermining against "we build, we do not unmake"; STD.CA.15/16 give Guild `-1` for taking what others gathered/built).

**The gap, per Andy (S141):** this may not be applied consistently — checked against the 5 identity-hidden CA cards that prompted the original question, only 2 (CA.15/16) actually carry the expected Guild penalty; **CA.9 Fund and CA.14 Disprove have no Guild portrait entry at all**, and **CA.11 Tort Interference has `portrait = {}`, no faction entries at all**. Whether those absences are each independently justified (matching the existing "no doctrinal stake" absence-justification convention used elsewhere on these same cards) or represent a real, systematic under-application of the off-doctrine-penalty principle is the open question. Andy's hypothesis: **this might be a gap across the whole card corpus**, not just these 3 cards — i.e., other cards where a faction's doctrine is in tension with the action it enables may be similarly missing the expected portrait penalty.

**Status:** Open, evidence-gathering (3 confirmed candidate-gaps: CA.9, CA.11, CA.14) — same organic-accumulation treatment as other categories in this log. Watch for more "faction acting against own stated doctrine, no portrait entry present" instances while reviewing the rest of the CA/PA set (Directorate onward) and note them here rather than deciding case-by-case. Not proposed as a fix; needs the fuller landscape before Andy can call whether this is systematic.

---

### 24. Category: entire Standard set missing the `card_id` field — confirmed corpus-wide, not just a "pending 04-n70" placeholder

**Surfaced by:** re-derivation pass (S141), after loading `design_reference_card_system.md` in full. §6 Field Groups (Identity) defines **two separate fields**: `card_id` (canonical `[FAC].[TYPE].n` ID, e.g. `"GHO.CA.4"`) and `id` (legacy sequence integer, e.g. `id=19`, "preserved for traceability" — implying it should hold an old numeric-era value, not the canonical string). Grepped all of Part2–4c: `card_id=` appears 132× in Part3 (Ring Modifiers, matches the 132-card set) and 26–30× in Part4a/4c (Guild/Directorate faction sets) — **zero times in `Part2_Standard.md`**. Every one of STD.CA.1–16 (and presumably the Standard PA set, not yet checked) uses only `id = "STD.CA.1"` — putting the canonical string ID in the legacy field and omitting `card_id` entirely.

**Distinction from the existing generic flag:** every Standard CA card's checklist already carries `Data schema validation ⚠ Pending 04-n70` — but that placeholder has been treated as a known, generic, already-tracked non-issue (per this pass's own §4 in `ca_pa_review_notes.md`). This is a specific, concretely-verified instance of what that placeholder is actually hiding, not a new category of gap — but it's more actionable than the placeholder suggested, since it's now grep-confirmed corpus-wide across an entire Part file rather than a vague "TBD."

**Status: Closed S148 (PM05 04-n192/Phase 1, PM02 L319).** Re-checked corpus-wide before fixing, not just the Standard CA set this item names — found the gap was much larger than scoped: Standard PA already had `card_id` (added since this item was written), but **40 faction CA cards were also missing it** (Guild 10/10, Directorate 8/8, Network 7/7, Syndicate 8/8, Ghost 7 of its CA set) plus **one Ring Modifier** (Overture/STD.MOD.1). All 16 Standard CA + 1 Ring + 40 faction CA = 57 cards fixed, `card_id` added mirroring each card's own `id` value. **Two exceptions left unfixed, flagged not guessed:** Ghost's **Backdate** and **FieldVerification** carry a placeholder `id = "Ghost-ext-TBD"` (not a real canonical ID) — assigning a real `card_id` would mean inventing the correct final numbering, a content decision (both are pre-existing legacy/blocked cards, Backdate is 🚫 BLOCKED per GR 7.2b), not a mechanical field-add. Fence (776)/`Card()` (385) counts unchanged; monolith regenerated.

---

### 34. Full CA corpus synthesis (S141) — cross-cutting patterns across all 69 cards, all 6 sets

**Scope:** Standard (16) + Directorate (8) + Ghost (15) + Guild (10) + Network (8) + Syndicate (12) = 69 CA cards, the complete 09-16 CA phase. This entry looks across all six per-faction reviews (§5a–5g in `ca_pa_review_notes.md`) for patterns invisible at the single-set level, matching the treatment the ModReactCard corpus got in item 20.

**A. Every recurring defect category is FactionSpecific-only — Standard is clean of all of them.** Zero instances, across the entire Standard set, of: invalid Function values (#25), unregistered Subjects (#27), `flat=` misuse (#7), or the prose/code cost mismatch (#31). Standard's only real bugs were the untyped-cost-attribute gap (#22, confined to CA.1–5) and the missing `card_id` field (#24, corpus-wide but mechanical). Every genuinely *inventive* schema deviation — a new Function word, a new Subject, a new cost notation, a semantically-off Portrait field — happened in a faction-specific card. Plausible read: Standard cards get more eyes/precedent-checking (they set the pattern everyone else copies), while faction-specific cards are where individual authors reach for something new under less scrutiny.

**B. The prose/code cost mismatch (#31, 4 instances: NET.CA.4, SYN.CA.3/5/9) has a shared shape worth naming.** In every instance, the prose states a clean, memorable single-resource number ("Capital×5," "Exposure×2") that reads as the *headline* cost, while the actual code is cross-resource and the named resource's share is lower than claimed. Three of the four (SYN.CA.3/5/9) are cards whose Design Rationale explicitly compares cost to a sibling card ("Capital×5, the most expensive..."; "Capital×3 vs DIR.CA.1's Mandate×2"; "dual cost vs SYN.CA.3's Capital×5 only") — comparative framing that would break if the number changed. Working hypothesis, not confirmed: these cards were cost-balanced into cross-resource form at some point *after* the comparative prose was written, and the prose was never swept forward. If true, the fix isn't "correct 4 typos" — it's "any card with cost-comparison prose is a candidate for the same drift," which would be worth a targeted grep (`grep -B5 "Capital×[0-9]"` style) rather than assuming these 4 are exhaustive.

**C. Portrait `flat=` clusters specifically on "public effect, covert actor" cards.** SYN.CA.10 Accord Transfer and SYN.CA.11 Redline are both explicitly designed so "the effect is public... but the actor is covert" (both cards' own Design Rationale says this in nearly those words). Hypothesis: authors reach for `flat` ("fires on resolution regardless of submitter") specifically to model the *table's reaction* to a public event, without registering that this makes their own submitting faction's entry `flat`-typed too, when `submitter=` is likely what was actually meant for that one faction. This is a more specific, more actionable framing of item #7 than "flat is used on targets" — it's "flat gets reached for whenever a card's fictional premise is 'public outcome, hidden author.'" Worth checking any future Discovery/Corrupt/Redirect card with that same public-outcome-hidden-actor shape.

**D. Missing-field gaps (#24) are now fully mapped: universal except in cards touched at S111 or later.** Across all 69 cards, `ps_framing` and `boost` are missing on every card *except* ones explicitly redesigned S111+ (SYN.CA.10/11/12, GHO.CA.4/5). `card_id` (vs. legacy `id`) is missing on the entire Standard set and roughly half of Directorate/Ghost, present on Guild/Network/Syndicate's newer cards. This confirms the "vintage correlates with drift" finding from the ModReactCard pass (item 20A) generalizes to CA — but with a twist: **newer cards don't have fewer defects, they have different ones.** Old cards are missing required fields and use wrong taxonomy terms; new cards (Syndicate's S111–S130 cluster especially) have all the required fields but introduce brand-new notation styles (#22's third cost form) and semantically-imprecise Portrait fields (#7) that didn't exist in the older, more conservative cards. A sweep strategy that only targets "old = bad" would miss the new cards' issues entirely.

**E. Intel Token as cost (#10) is the single most-repeated finding of the whole pass — 9 instances, 4 of 5 factions.** No longer reasonably describable as drift; this is close to de facto design intent that the schema hasn't caught up to. Of everything in this log, this is the strongest single candidate for "stop flagging it as a violation and formalize it as a second cost category."

**F. One open thread from mid-pass not carried through to completion:** item #23 (portrait penalty for acting against own doctrine) was raised via a Guild-specific question during the Standard review and evidence-gathered against 3 Standard-set cards (CA.9/11/14). It was never systematically re-checked against the other four factions' *own* stated doctrines against their *own* cards — e.g., does Directorate ever get a portrait penalty for acting against "restraint and continuity," does Ghost ever get one for acting against "understanding must precede action"? That cross-check wasn't done this pass. Flagging so it isn't mistaken for closed — it's still a 3-card, Standard-only evidence base.

**Status:** Presented as a cross-cutting menu, same treatment as items 4 and 20's syntheses — none of A–F is a proposal, all await Andy's read on which (if any) warrant a dedicated normalization session before Directorate-onward PA review starts.

---

### 35. Category: cross-card "same cost/threshold as [CA card]" claim checked directly and found false — Standard set, not just FactionSpecific

**Surfaced by:** STD.PA.1 Open Operations and STD.PA.2 Disputed Claim (S142, Standard PA review, first PA set reviewed). Per the standing re-derive instruction (`ca_pa_review_notes.md` §2 — "a cross-card comparison in the card's own Design Rationale... open the other card and check"), both cards' claimed CA counterparts were opened directly:

- **STD.PA.1** claims "Public counterpart to STD.CA.3 (Campaign). Same cost (2 native)." Actual STD.PA.1 cost: `resource.faction(acting) * 2` (single-resource). Actual STD.CA.3 cost: `resource.faction(acting) * 1 + resource.district(native) * 1` (dual-resource). Not the same cost shape — the claim is false.
- **STD.PA.2** claims "Public counterpart to STD.CA.4 (Undermine). Same cost (2 native), slightly better base threshold (45 vs 40)." Actual STD.CA.4 cost is also dual-resource (same mismatch as above), and its actual threshold is **50**, not 40 — meaning STD.PA.2's 45 is *worse* (lower success chance), the opposite of "slightly better."
- **STD.PA.3** claims "Public counterpart to STD.CA.1 (Build Structure). Same cost" — checked directly, **confirmed true**: identical dual-resource expression on both cards, including the shared untyped-attribute bug (item #22).

**Why this matters:** this is the same failure mode the standard was written to catch (STD.MOD.1 Overture's factually-backwards claim, S140), now confirmed in the *Standard* set specifically — synthesis item #34-A's claim that "every recurring defect category is FactionSpecific-only, Standard is clean" was built on the CA-only corpus and doesn't hold once PA is included. 2 of 3 checked cross-card claims in Standard PA were wrong; the third was right — not a uniform pattern, genuinely needs the direct check every time, per the standing instruction.

**Status: Closed (Phase 5 / 04-n196, S150).** Both false claims rewritten standalone per the no-cross-reference rule (PM02 L276) — Design Rationale, design_note, and the Balance/Resource-cost-positioning checklist rows on STD.PA.1 and STD.PA.2 no longer name or compare against STD.CA.3/STD.CA.4 at all; each now describes its own cost/threshold/outcome directly (STD.PA.1: mono-resource 2 faction native, Automatic, PS+1; STD.PA.2: mono-resource 2 faction native, threshold 45). STD.PA.3↔STD.CA.1 (confirmed true) was left untouched — not a defect. Worth running the same direct-check discipline on every remaining "public counterpart to STD.CA.n" claim corpus-wide if a normalization pass ever revisits this, but per L276 the fix in all cases is removing the comparison, not verifying and keeping it.

---

### 36. Category: dangling "Cost reasoning" design_note fragment doesn't match the card's own cost — copy-paste artifact, recurring

**Surfaced by:** SYN.CA.4 (S141, first instance — "Cost reasoning: Exposure and Findings..." appended to a design_note whose actual cost is Capital-only). **Now 2 more confirmed instances (S142, Directorate PA review):** DIR.PA.1 Regulatory Override's design_note ends "...Cost reasoning: Exposure is necessary to enforce the controls publicly..." though its actual cost is Mandate-only; DIR.PA.2 Convene an Inquiry's ends "...Cost reasoning: Findings provide the legal precedent and evidence required to sustain the injunction long-term" though its cost is Mandate-only *and* "the injunction" names a different card entirely (DIR.PA.6 Standing Injunction, not DIR.PA.2 itself).

**Why this matters:** DIR.PA.2's fragment is the clearest evidence yet that these are literal copy-paste artifacts, not just stale self-reference — it references another card's own mechanic by name. All three instances share the exact shape: a trailing "Cost reasoning: [resource] is/provides [justification]" sentence bolted onto an otherwise-coherent design_note, naming a resource type the card doesn't actually use.

**Status: Closed — turns out to have already been fixed (S150 check against Phase 5 / 04-n196).** This entry duplicated ground already covered: all 3 mismatched instances (SYN.CA.4, DIR.PA.1, DIR.PA.2) were fixed in S146 under PM05 04-n186 / PM02 L290 (a corpus-wide grep run for the same defect that day found 24 total "Cost reasoning" instances corpus-wide, confirmed 5 wrong — these 3 plus NET.CA.6 and DIR.CA.2 — and removed the mismatched fragments). This log entry was never marked closed when that happened, so it re-surfaced as open scope in Phase 5. Verified directly against current file state (S150): all 3 `design_note` fields are clean, checklist rows already note the resolution with the 04-n186 citation. No further action. **Contrast cases (correct-and-left-alone):** GHO.PA.4, GUI.PA.1/3/4/5 all carry the same template correctly, confirmed against their own cost fields.

---

### 37. Category: `resolution_type = "Permanent public act"` — not in the confirmed str vocabulary

**Surfaced by:** DIR.PA.5 Zoning Freeze, DIR.PA.6 Standing Injunction, and DIR.PA.11 Public Hearing (all S142, Directorate PA review — all three are Permanent-persistence card-as-condition PAs). design_reference_card_system.md's confirmed `resolution_type` vocabulary is exactly two values: `"Probabilistic"` (d100) and `"Transactional"` (Automatic) — "Permanent public act" is neither, and doesn't appear anywhere in the confirmed vocabulary list.

**Why this matters:** all three cards using this value are `resolution = Automatic`, which per the documented rule should take `"Transactional"`. But all three are also specifically the Permanent/card-as-condition pattern, distinct in kind from a normal one-shot Automatic PA (STD.PA.1, STD.PA.7, etc.) — it's plausible an author deliberately reached for a third category to distinguish "this card IS a standing condition" from "this card resolves once and is done," and that distinction may be worth keeping, just not under an unconfirmed ad-hoc string.

**Status:** Superseded by the broader finding at item #41 — "Permanent public act" turns out to be one of *nine* undocumented `resolution_type` values in active use, not an isolated Permanent-PA gap. See #41.

---

### 38. Category: `outcome_type` missing or explicitly `None` on real PublicAct cards — confirmed defects from the PA-phase's flagged highest-priority check

**Surfaced by:** DIR.PA.10 Official Demonstrations (`outcome_type = None`, explicit) and DIR.PA.7 Curfew + DIR.PA.8 Subpoena (field absent entirely) — all S142, Directorate PA review. Per §6.2's data dictionary (flagged going into the PA phase as the single highest-yield schema check, `ca_pa_review_notes.md` §4): "Public act resolution process type; None for covert operations" — the clear implication being every PublicAct should carry a real value. DIR.PA.10 has a fully structured d100 resolution with all four tiers populated and no ambiguity about what kind of outcome it produces (a self-contained PS shift, matching its siblings' `Unilateral` value) — there's no structural reason for it to be `None`. DIR.PA.7/8's omission is part of their broader thin-stub incompleteness (see #29), not an isolated finding.

**Status: Closed (Phase 5 / 04-n196, S150).** **DIR.PA.10 fixed** — `outcome_type = None` → `Unilateral`, matching every sibling Directorate PA card (self-contained PS shift, all four tiers populated, no structural reason for `None`). No `card_status` sync needed — `outcome_type` isn't a tracked DB column. **Remaining 8 instances (GUI.PA.3/4/5/6/7/8 + DIR.PA.7/8)** were folded into #29 and closed there — `outcome_type` filled on all 8 as part of that item's full content rewrite (their `success`/`restriction` fields needed real structured effects before a real `outcome_type` could be determined anyway). Confirms the flagged check is genuinely productive but its yield correlates with content maturity (clean on well-developed cards, defective on thin stubs) rather than faction.

---

### 39. Category: `narrative`/`perspectives` fields entirely absent (not just sparse) — uniform across one S131 authoring cluster

**Surfaced by:** DIR.PA.4, DIR.PA.5, DIR.PA.9, DIR.PA.10, DIR.PA.11 (all S142, Directorate PA review) — all five are S131-vintage redesigns/new cards (v0.1 or v3.0) that each explicitly set `narrative = None` and `perspectives = None`. Every other card reviewed in the CA/PA phase so far (114+ cards) carries at least a Directorate-only `perspectives` entry, even on faction-specific cards with no aligned/opposed voice to show. This 5-card cluster is the only place in the corpus where both fields are uniformly, deliberately absent.

**Why this matters:** this isn't scattered drift — it's uniform across exactly the 5 cards from one authoring batch (all closing specific named PM05 gaps: 04-n104, 04-n89, 04-n108, 04-n142, the 54-card floor), suggesting the batch was written to solve structural/schema problems first, with narrative voice deferred wholesale rather than per-card. All 5 also lack a Card Story block (consistent with the corpus-wide `04-n79` pending status), but go further than that — even the base `perspectives` dict, which doesn't depend on Card Story being written, is missing.

**Confirmed as a real gap, not a stylistic option (Andy, S142):** the `perspectives` field on FactionSpecific cards is meant to carry three voices — the acting faction's own, one aligned, one opposed — as deliberate worldbuilding, on *every* FactionSpecific card (now documented in `ref_card_types.md`). This cluster's `perspectives = None`, along with the same gap on the genuinely-thin Guild/Network/Syndicate PA stubs (schema_cleanup_log.md #29's cluster — GUI.PA.3–8, NET.PA.4–6, SYN.PA.4/5, all missing `narrative`/`perspectives` too), is the same underlying defect repeating across every faction's PA stub batch, not five isolated cases. Roughly a third of the 45-card PA corpus is missing this by default.

**Status: Closed (S150).** The genuinely-thin stub cluster referenced above (GUI.PA.3–8, NET.PA.4–6, SYN.PA.4/5) had its `narrative`/`perspectives` fields written as part of item #29's full rewrite. The remaining 5 — DIR.PA.4, DIR.PA.5, DIR.PA.9, DIR.PA.10, DIR.PA.11 — each got a standalone `narrative` line and a 3-voice `perspectives` dict (Directorate acting + one aligned + one opposed, per `ref_card_types.md`), written fresh per card rather than swept-forward text. Card Story prose blocks remain pending 04-n79 for all 5 — out of scope here, tracked separately. Both Design checklist Voice fit and Data schema validation rows updated on all 5 cards to reflect the populated fields.

---

### 40. Category: "(stub)" header label stale on cards with complete content; DB `design_pass` flag desynced from blank .md Status table

**Surfaced by:** DIR.PA.4, DIR.PA.5, DIR.PA.9, DIR.PA.10, DIR.PA.11 (all S142, Directorate PA review) — all five carry a `*(stub)*` header suffix and a fully-blank 17-row checklist template, yet all five have complete, schema-conformant `Card()` blocks (structured cost/success/portrait, detailed design_note) representing real S131 design work that closed specific named gaps. Only DIR.PA.7 and DIR.PA.8 in the same file are genuine stubs matching their label (bare prose-string `success`, most fields absent). The "(stub)" header is not a reliable signal of content maturity in this file — 5 of 7 stub-tagged cards are stale-labeled.

**Compounding DB desync:** DIR.PA.4 and DIR.PA.5 specifically showed `design_pass=1` in `card_status` *prior to this session's review*, despite their .md Status tables being fully blank (no Design Pass ✓ anywhere) — a second, distinct DB/MD drift pattern alongside item #30's NULL-taxonomy version (there: DB missing data the .md has; here: DB claims a review-completion state the .md doesn't reflect at all).

**DB desync portion closed S148 (Phase 2 / 04-n193, PM02 L322) — moot, not synced.** Andy's ruling: since schema cleanup is ongoing and a full design-review re-do is coming once it's done, `design_pass` (along with `issues_resolved` and `signed_off`) is reset to `0`/blank corpus-wide, both `.md` and DB — nothing is considered reviewed/resolved/signed-off until that re-do happens. This resolves the DIR.PA.4/5 desync by making the question moot rather than reconciling a stale value. **The "(stub)" header mislabeling itself remains open** — cosmetic, not touched by this reset; still needs its own pass whenever a normalization sweep is scheduled.

---

### 41. Category: `resolution_type` vocabulary is far larger than documented — 9 values in active use, only 2 confirmed in schema

**Surfaced by:** DIR.PA.5/6/11's `"Permanent public act"` (originally logged narrowly as item #37) plus, on closer look during the Network PA review (S142), **NET.PA.1's `resolution_type = "Contested"`** — which prompted a full corpus grep rather than treating it as one more isolated instance. `design_reference_card_system.md` documents exactly two confirmed values (`"Probabilistic"` for d100, `"Transactional"` for Automatic). The actual corpus (`grep -rn 'resolution_type\s*=\s*"' *.md | sed -E 's/.*"([^"]+)".*/\1/' | sort | uniq -c`, all 8 Part files) shows:

| Value | Count |
|---|---|
| Transactional | 213 |
| Probabilistic | 53 |
| Positional wager | 14 |
| Contested | 10 |
| Permanent public act | 7 |
| Conditional | 4 |
| Deceptive | 2 |
| Predictive | 2 |
| Verification | 2 |
| PlayerChoice(target) | 1 |

**This is not a fresh discovery — it's an already-tracked, currently-open PM05 item.** `PM05___Active_Punch_List.md` item **04-25** ("Resolution type field — rationalize taxonomy after full card set") already names this exact gap: "C01–C15 populated with descriptive prose values — no enforced vocabulary... Known values in use: Transactional, Probabilistic, Positional wager, Conditional... Once C01–C35 and P01–P18 are complete: review all Resolution type values across the full set; rationalize into a formal taxonomy." **The CA/PA design review (09-16) is now that trigger condition** — C01–C35 and P01–P18 (i.e., the full CA+PA corpus) are at or near complete as of this session. PM05 item 04-n36 also separately pre-registers `"PlayerChoice(target)"` as an intentional future value for a specific card family (Directorate formal-inspection cards), confirming at least one of these 9 values is deliberate, not drift.

**Corpus-mismatch caught in this review specifically:** STD.PA.2/4/5/6 (all `"Contested"`) were reviewed in the Standard PA pass (§5h) without this being flagged — caught retroactively only once NET.PA.1's identical value prompted the full grep. Those four cards' checklists have been corrected after the fact. Worth treating as a lesson: a value not matching the documented 2-item list should always be checked against the corpus, not assumed to be an isolated typo, precisely because 04-25 already exists and this pass is its natural trigger.

**Status: Closed S147 — closes PM05 04-25.** Andy's call: evaluate each of the 9 values against a discriminating test (does deleting it lose information no other field carries?), convert the field from `str` to a real enum. Re-audited before starting — corrected count was 161 live instances (raw grep of 308/170 in earlier snapshots double-counted checklist prose that cites the values in backticks; deduplicated by card). Result: `ResolutionType: Probabilistic | Transactional | PositionalWager` — 3 confirmed values, written into Art 04 §6.3 (`Part1_Core.md`), field type changed `str | None` → `ResolutionType | None` (§6.1/§6.2). Per-value disposition: **Probabilistic**/**Transactional** unchanged (base 2, `resolution = d100`/`Automatic` respectively) — 2 stray Ghost cards found mislabeled `Transactional` despite `resolution = d100` (GHO.PA.3, GHO.PA.5), corrected to Probabilistic. **PositionalWager** confirmed genuine (8 cards, all `resolution = Automatic`, all modify a not-yet-revealed future beat's submission slate — verified via full card read on STD.CA.6/7, no other field captures this) — STD.CA.7 was a 6th mislabeled instance found via its own self-flagged checklist note ("may be a misnomer... same positional-wager behavior as STD.CA.6"), corrected. **"Contested"** (5 cards) collapsed to Probabilistic — only 1 of 5 (STD.PA.2) actually placed a `ContestedMarker`; the label didn't correlate with a real mechanism across its own cluster. **"Permanent public act"** (3 cards, DIR.PA.5/6/11) collapsed to Transactional — fully redundant with `persistence = Permanent`, already present on all 3. **"Predictive"** (1 card, GHO.CA.1) collapsed to Transactional — `resolution = Automatic`, deterministic declare-then-verify check, not a distinct mechanism (the card's own perspective text explicitly denies the framing: "We are not predicting"). **"Conditional"** (GHO.MOD.1), **"Deceptive"** (Backdate), **"Verification"** (Field Verification) left as non-conforming string values, untouched — tied to already-broken (item #12, invalid `resolution=Prediction`) or `🚫 BLOCKED` (PM05 04-n103, GR 7.2b) cards; their eventual resolution_type isn't decidable until those separate fixes land. **"PlayerChoice(target)"** — confirmed never actually used anywhere in the corpus; only ever appeared inside one checklist note's example list. Every affected checklist row (STD.PA.2/4/5/6, NET.PA.1, DIR.PA.5/6/11, STD.CA.7, STD.PA.8, GHO.CA.1) rewritten to state the correction rather than the stale open question. Monolith regenerated; fence/`Card()` counts verified clean across all 8 Part files. Full detail: PM02 L299.

---

### 43. Category: `cost` expression draws from the *target* faction's resource pool, not the acting faction's own — blurs cost/effect distinction

**Surfaced by:** NET.PA.5 Viral Outrage (S142, Network PA review, thin stub). `cost = resource.faction(Network).exposure * 2 + resource.faction(target_faction).native * 1` — the second term draws from `target_faction`'s own native resource pool, not the acting faction's (Network's). Every other cost expression reviewed across the full CA/PA corpus (114+ cards) is paid entirely from the acting faction's own pool; §6 Field Groups defines `cost: CostExpr` without addressing whose pool it draws from, implicitly assuming the submitter's. The card's own design_note frames this as deliberate ("Network burns the opponent's own native resource to fuel the smear campaign") — this isn't an authoring slip, it's an intentional mechanic that the schema has no vocabulary for.

**Why this matters:** if a resource deduction from a non-acting faction is a valid `cost` term, that's a new category needing its own confirmed syntax and procedural backing (does the target faction have any say, is this even legal if they don't hold enough of that resource, what happens on partial availability). If it's not intended as `cost` at all, the mechanic probably belongs in `success` as an explicit effect on the target (`faction(target_faction).resource(native) -= 1`) instead, keeping `cost` scoped to what the submitter pays. Either way, this is a schema question, not a typing question — distinct from item #22 (untyped attribute) and item #10 (Intel Token as cost), both of which are about *what* a card pays, not *whose pool* it pays from.

**Status:** Open, single confirmed instance, on a genuinely thin stub card (not yet fully designed). Not fixed — flagged per scope. Worth watching for recurrence in the remaining PA sets (Syndicate) before treating this as an isolated card-specific idea vs. a broader "cost can draw from a target" pattern that needs formal support.

**Status: Closed S148.** Andy's ruling: cost is always paid from the acting (submitting) faction's own resource pool, no exception — `faction.target.native` (or any relative term) only ever resolves *which* resource type is owed, never *who* pays it. NET.PA.5 follows the same "prior economic embedding" shape already established by GHO.CA.4 (item #43 was surfaced independently of that card, but is the identical mechanic): Network must already hold a unit of the target faction's native resource for the cost to be payable — it is not drawn from the target's current holdings. `cost` rewritten to the new canonical syntax (item #47) — `Exposure * 2 + faction.target.native * 1`; `design_note` reworded to state the prior-holding requirement explicitly (previously read "Network burns the opponent's own native resource," which invited the wrong reading); both flagged checklist rows ("Supported by game procedure," "Resource cost positioning") corrected and flipped ⚠→✓. No new schema vocabulary needed — this was a checklist/design_note misreading of already-confirmed CostExpr semantics, not a genuine gap.

---

### 44. Category: `persistence = Transient` combined with prose describing an effect spanning a Quarter boundary — tension with the "no multi-Quarter temporaries" rule

**Surfaced by:** DIR.PA.7 Curfew (S142, Directorate PA review — "Places a Standing Condition on target_district until the end of Quarter+1") and **SYN.PA.5 Protection Racket** (S142, Syndicate PA review — "until Quarter+1"), both genuinely thin stub cards. Art 04 §5's duration discipline (P19–P21, design_reference_card_system.md) states "Effects are permanent or within-Quarter — multi-Quarter temporaries prohibited." `Transient` is documented as a valid persistence value (Immediate/Transient/Seasonal/Permanent), but both cards pair it with prose describing the effect lasting into the *following* Quarter — which reads as exactly the prohibited shape, if "Quarter+1" means what it appears to mean (the next Quarter, not "the remainder of this Quarter").

**Why this matters:** both instances are on thin, unstructured stub cards (bare-string `success`), so it's plausible this is just loose, not-yet-formalized prose rather than a deliberate design choice — unlike the "Permanent public act" resolution_type question (item #41), which recurred on well-developed cards. Still, two independent cards using the identical "Quarter+1" phrasing is enough to flag as a pattern rather than a one-off typo, especially since neither card defines what "Transient" actually bounds without a structured `persistence_condition`.

**Status: Closed (Phase 5 / 04-n196, S150).** No corpus-wide "Quarter+1 means remainder of this Quarter" shorthand was found — the two cards turned out to have genuinely different design intent, ruled separately by Andy:

**SYN.PA.5** — the "Quarter+1" framing was an over-reach, not real design intent. Fixed to `persistence = Seasonal` (within-Quarter only), prose corrected to "for the remainder of the Quarter." Playing it in Month 3 gives no benefit — an intentional early-play tradeoff, not a defect.

**DIR.PA.7** — genuinely needs to survive past this Quarter's end: Deployment Markers are only placed during the Placement phase, which happens once per Quarter before Month 1, so a within-Quarter-only duration would clear before the window the card exists to affect ever occurs. Resolved without a P19 rule exception by reclassifying as `persistence = Permanent` with a `persistence_clearing_trigger` that fires once the next Placement phase resolves (same card-as-condition mechanism as DIR.PA.5/DIR.MOD.9). **New, unconfirmed §6.3 vocabulary introduced:** `placement_phase.completed()` — no existing TriggerExpr covers "the Placement phase has resolved"; flagged the same way as other new-primitive introductions in this corpus (e.g. #26's `game.active_permanents`), not silently assumed confirmed. `persistence_effect` itself remains prose, not a structured MutationExpr — full structuring deferred to #29, since DIR.PA.7 is also in that stub-rewrite list.

---

### 45. Full PA corpus synthesis (S142) — cross-cutting patterns across all 45 cards, all 6 sets

**Scope:** Standard (8) + Directorate (11) + Ghost (5) + Guild (10) + Network (6) + Syndicate (5) = 45 PA cards, the complete 09-16 PA phase, mirroring item #34's treatment of the CA phase. Cross-references `ca_pa_review_notes.md` §5h–§5m (per-faction findings).

**A. Item #34-A's "Standard is clean of all FactionSpecific-only defect categories" claim does NOT survive the PA phase.** Standard PA turned out to be the single dirtiest set on two specific measures: the untyped-cost-attribute bug (#22) is 8/8 in Standard PA (vs. 6/16 in Standard CA), and 2 of 3 checked "same cost/threshold as the CA counterpart" cross-card claims (#35) were confirmed false — both defects the CA-only synthesis had pinned as either Standard-clean or FactionSpecific-only. The lesson isn't "Standard is secretly bad" — it's that a synthesis built on half a corpus (CA only) doesn't generalize to the other half, and shouldn't have been trusted to. Every remaining synthesis claim in this item should be read the same way: current best evidence, not a settled fact.

**B. The bare-string/undeveloped-stub defect (#29) exploded in scale and moved into new fields.** CA phase: 5 instances, all `success` fields, all clustered in Ghost/Guild CA stubs. PA phase: 12 instances (now 12 total across both phases, entirely new in PA), spreading to `restriction` (GUI.PA.6/7), `persistence_condition`/`persistence_effect` (NET.PA.3), and a distinct sub-shape — syntactically-valid Python left inside quotes rather than English prose (GUI.PA.4). Guild PA alone contributed 6 of the 12 total instances (the largest single-set concentration in the whole review) — Guild's PA stub cluster is bigger and more varied in failure mode than any CA-phase stub cluster.

**C. The "(stub)" header label is not a reliable signal either way — it has to be checked every time, in both directions.** Directorate PA: 5 of 7 stub-tagged cards were actually complete (mislabeled). Guild PA: 6 of 6 stub-tagged cards were genuinely thin (accurately labeled). Network PA: 3 of 3 accurately labeled. No faction-level or corpus-level prior predicts which way a given "(stub)" tag will go — this confirms the direct-check discipline (§2 of `ca_pa_review_notes.md`) was necessary, not belt-and-suspenders.

**D. `resolution_type` vocabulary sprawl (#41) was invisible until this phase, and directly triggers an already-open PM05 item.** 9 values in active use against 2 documented — caught only because NET.PA.1's `"Contested"` prompted a full grep, which then required retroactively correcting the Standard PA review (§5h) where the same value had already passed through unflagged. This is the single most consequential finding of the PA phase: it feeds directly into PM05 item 04-25, whose stated trigger condition ("once C01–C35 and P01–P18 are complete") is essentially met now.

**E. Intel Token as cost (#10) kept growing and diversifying, not converging.** 10 instances/4 notations at the end of the CA phase → 14 instances/7 notations at the end of the PA phase. No two cards across the entire 114-card corpus spell this identically. This is now unambiguously the single strongest formalization candidate in the whole log — not because any individual instance is a "violation" needing a fix, but because the sheer notational chaos (7 ways to say the same thing) is itself evidence the schema never gave authors a canonical form to converge on.

**F. `card_status` DB/MD desync is a recurring, multi-field, multi-faction pattern, not isolated incidents.** Four separate instances surfaced this session alone across three fields (`design_pass`, `issues_resolved`, `layer`/`function`/`subject`) and three factions (Directorate, Ghost, Guild) — on top of the two CA-phase instances (item #30). This reads as a systemic gap in whatever process is supposed to keep `card_status` synchronized with the `.md` source of truth, not five unrelated one-offs — worth a dedicated reconciliation pass rather than continuing to log individual instances as they're found.

**G. The dangling "Cost reasoning:" design_note fragment (#36) turned out to be a coin-flip, not a defect signal.** 3 confirmed mismatches (SYN.CA.4, DIR.PA.1, DIR.PA.2) vs. 5 confirmed-correct instances (GHO.PA.4, GUI.PA.1/3/4/5) found in the same pass. The phrase itself predicts nothing — every instance still needs the direct check, same lesson as item A above at a smaller scale.

**H. `flat=` portrait misuse (#7) is the one pattern that stayed genuinely faction-specific.** All 5 confirmed instances across both CA and PA phases are Syndicate cards (SYN.CA.7/10/11/12, SYN.PA.3) — no other faction shows this pattern anywhere in 114 cards. Unlike items A/B/C above, this one held up under the full-corpus check rather than collapsing.

**I. Corpus-wide scaffolding debt closed this session:** all 45 PA cards now carry `card_id`/`boost`/`ps_framing` explicitly (previously silently absent on roughly two-thirds of the set), matching the same fields' state in the Ring Modifier and faction ModAction/ModBattle corpora. All 45 now show `design_pass=1` in both `.md` and DB.

**Status:** Presented as a cross-cutting menu, same treatment as items 4, 20, and 34's syntheses — none of A–I is a proposal, all await Andy's read on which (if any) warrant a dedicated normalization session. Items D and E (resolution_type, Intel Token) are the two strongest candidates for near-term attention given they each connect to a concrete, already-recognized need (04-25's trigger condition; a de-facto second cost category). With this item, the full 09-16 design review pass (114 CA/PA cards + earlier ModReactCard/ModAction/ModBattle corpora) has now had a cross-cutting synthesis written for every phase.

**Addendum, checked after Andy's follow-up prompt (S142):** three further observations, verified by direct grep across all 6 PA files rather than recalled from memory:

- **`outcome_type` enum coverage is far narrower in practice than in schema.** §6.3 documents six values (`Binary`, `ElectPlayer`, `ElectDistrict`, `ElectFaction`, `BilateralAgreement`, `Unilateral`). Actual usage across all 45 PA cards: `Unilateral` ×24, `BilateralAgreement` ×2 (STD.PA.8, GUI.PA.2), `ElectPlayer` ×2 (SYN.PA.1, SYN.PA.3), `None` ×15 (mix of this session's scaffolded-placeholder `None`s on thin stubs and the confirmed real gaps in item #38). **`Binary`, `ElectDistrict`, and `ElectFaction` have zero instances anywhere in the PA corpus.** Either these three values describe design space nobody has built yet (plausible — a PA that lets a target pick among several *districts* or several *factions* to redirect an effect at is a real, unbuilt card shape), or they're aspirational/theoretical enum entries. Worth Andy's read on whether any planned-but-unwritten PA cards were meant to use them.
- **`boost` is essentially a dead field in the PA corpus.** Of 45 cards, only **GHO.PA.3** uses it for real (expired Intel Tokens scaling a BM-xx multiplier). Every other PA card's `boost` is `None` — either originally authored that way or scaffolded so this session. Contrast with the CA corpus, where DIR.CA.5 Sanctioned Raid uses `boost` as a core mechanic (scaling threshold and effect together). This may be structurally sound — PAs are already public/declared, so the "cannot see how big this move is until it resolves" tension `boost` seems designed for is a covert-ops concern — but it's worth naming explicitly rather than letting a fully-specified field sit unused across an entire card-type category without anyone having decided that's fine.
- **Card Story (P26) compliance across the PA corpus is very low** — the large majority of PA cards still carry "⚠ Story pending 04-n79" rather than real Card Story text (already-tracked via 04-n79, not a new gap) — but worth stating the scale plainly now that the full PA set has been read end to end: this is one of the largest concrete blockers standing between "Design Pass ✓" (done this session, corpus-wide) and any future sign-off pass, since P26 is a locked principle (S78) that a real design problem exists if the story can't be told plainly.

**Root-cause note on item #35 (cross-card claim mismatches) — superseded, see item #46.** The "public counterpart to STD.CA.n" relationship that produced 2 false claims in Standard PA is not a one-card fluke — it recurs throughout the PA corpus (STD.PA.1/2/3↔STD.CA.3/4/1, SYN.PA.1↔SYN.CA.3, GUI.PA.2 distinguished from STD.CA.9, DIR.PA.9/10 both cross-reference DIR.CA.6/7's counting mechanism) as a design methodology, not incidental prose. This entry originally floated a lightweight structured field to formalize the pairing for traceability. **Andy's direction (PM02 L276): the opposite — remove the cross-referencing practice from card content entirely, don't formalize it.** Cards must stand on their own; comparison/contrast across cards belongs in a separate strategy-guide-style artifact, if wanted at all, not inside individual card design_rationale/design_note/arbiter_note. See item #46.

---

### 42. Category: `card_status` DB/MD desync family — running tally across the PA phase

Consolidating the desync instances found across this session rather than scattering them further: STD.CA.12/GUI.CA.10 (S141, `layer`/`function`/`subject` NULL in DB despite valid code — #30), DIR.PA.4/5 (`design_pass=1` in DB despite blank .md Status — #40), GHO.PA.3/4/5 (`issues_resolved=1` in DB despite blank .md Status — noted under #40), GUI.PA.10 (NULL taxonomy again, second Guild-set instance — #30 extended). Four distinct fields now show the same underlying shape (DB claims a state the .md doesn't reflect), across 3 factions and both CA/PA phases. Not itself a new item — pointer for whoever eventually reconciles `card_status` against the `.md` source of truth, so all instances are found in one pass rather than trickling in card-by-card.

**Status:** Open. Andy's S142 direction: scaffold-all is the scope of this pass regardless of stub/non-stub status — applied here (all 5 mislabeled cards got full checklists this session; DIR.PA.7/8 also got full checklists per the same instruction, see item #29). Header text itself ("*(stub)*") was not corrected — flagged, not fixed, per scope; worth a quick pass to drop the stale label from DIR.PA.4/5/9/10/11 once a normalization sweep is scheduled.

**Recurred on a different field (S142, Ghost PA review):** GHO.PA.3, GHO.PA.4, GHO.PA.5 all show `issues_resolved=1` in `card_status` despite their .md Status tables showing a fully blank Issues Resolved cell — same desync shape as DIR.PA.4/5, but on `issues_resolved` rather than `design_pass`, and in a different faction/phase entirely. Now 5 confirmed instances of "DB flag claims a review-completion state the .md doesn't reflect" across 2 factions. Not reconciled either direction — flagged only, per scope.

**Status: Closed S148 (Phase 2 / 04-n193, PM02 L322).** Picked back up cold to actually execute the reconciliation this item was tracking. Full corpus re-derivation (not the named handful) surfaced far more drift than scoped — a DB infrastructure incident forced a full restore first (PM02 L321), then the real reconciliation: **190 initial field-level mismatches**, almost all `design_pass`/`issues_resolved` (the DB had simply gone stale against months of review-pass updates to `.md`, corpus-wide, not a handful of named cards). Andy's ruling made the direction question moot for both fields (and `signed_off` besides): reset all three to `0`/blank, both `.md` and DB, since nothing is considered reviewed/resolved/signed-off again until the schema cleanup finishes and a full design-review re-do happens. That left exactly 7 taxonomy (`layer`/`function`/`subject`) mismatches, resolved under item #30 (3 genuine DB-stale rows synced; 4 were reconciliation-script false positives, both sides already agreed). One incidental fix along the way: **GHO.CA.11** (Signals Analysis) had a literal `id=TBD` placeholder in `.md` despite DB already carrying the real `GHO.CA.11` assignment — corrected in `.md` to match, closing a write-back gap its own checklist had already flagged. Corpus baseline is now **386** `Card()` blocks (was 385 — that GHO.CA.11 fix corrected a bare `Card(` with no preceding variable name, which had been silently undercounted).

---

### 46. Category: `affinity` cost-waiver clause references a `resource.district(...)` cost term that doesn't exist on the card

**Surfaced by:** GUI.PA.1 Civic Works Mandate (S148, found while fixing schema_cleanup_log #22's district-notation bug). `affinity = faction(Guild): cost.resource.district(native) = 0  # waived for both districts` — and the card's own Balance checklist row states "both district natives waived (Guild)" — but the actual `cost` field is `resource.faction(Guild).capacity * 2 + resource.faction(Guild).capital * 1 + resource.faction(Guild).mandate * 1`, with no `resource.district(...)` term anywhere for the affinity clause to zero out.

**Not fixed:** two plausible readings, neither decided here — (1) the cost once had a district-native component that was removed in a later edit, and `affinity`/Balance simply weren't updated to match; (2) the affinity/Balance language is a stale copy-paste from a sibling card and never described this card's real cost at all. Distinguishing between them (and deciding whether to add a district term back to `cost`, or strip the affinity clause and Balance language instead) is a content decision, not a notation fix.

**Status: Closed S148.** Reading (1) confirmed: `design_note` ("Cost reasoning: 2 Capacity + 1 Capital + 1 Mandate (Ceiling-tier). Capital secures the massive land footprint, while Mandate bypasses zoning laws...") and the Resource cost positioning checklist row (already ✓, already cross-checked against `design_note` and confirmed matching the real `cost` field) both describe the *current*, correct cross-resource cost — only the Design Rationale prose, Doctrine alignment row, Balance row, and `affinity` field itself were never updated after that redesign, all three still describing a stale "4 Capacity, district natives waived" model with nothing left in the actual cost to waive. **Broader schema point, Andy's call:** `affinity` only has meaning on a Standard card (`subtype = Standard`, `faction = All`) — it exists to differentiate how the *same* card plays out depending on which faction submits it, and a FactionSpecific card has no "other faction" to differentiate against, so `affinity` should be `None` there unconditionally, by schema rule rather than per-card judgment. Corpus-checked before writing the rule into §6.1/§6.2 (`Part1_Core.md`): GUI.PA.1 was the *only* non-`None` `affinity` instance anywhere in the 5 faction Part files — Ghost/Directorate/Network/Syndicate all already conform, no other sweep needed. Fixed: `affinity = None`; Design Rationale, Doctrine alignment, and Balance rows corrected to state the real 2 Capacity + 1 Capital + 1 Mandate cost instead of the stale figure. Monolith regenerated; fence (776)/`Card()` (385) counts unchanged.

---

### 47. Category: `CostExpr` surface syntax simplified — corpus not yet swept to match

**Surfaced by:** direct review of the `CostExpr` §6.3 entry (S148, same session that closed item #22). The confirmed grammar written for #22 (`resource.faction(X).type * n` / `resource.district(Y).native * n`) was replaced with a terser canonical form: bare `ResourceType * n` (`Capital`, `Mandate`, `Exposure`, `Findings`, `Capacity`) for a fixed type regardless of scenario, and dot-chain `faction.X.native * n` / `district.Y.native * n` (not nested function calls) for a type that resolves relative to whichever faction/district X/Y is at play time.

**Not yet done:** the ~150 existing `resource.faction(...)` cost/boost instances and ~28 `resource.district(...)` instances across the corpus still use the prior nested-call form. `§6.3` documents the new canonical syntax for cards written going forward; the existing corpus has not been renormalized to match.

**Addendum (S148):** a further simplification rule, confirmed against NET.PA.5 (closed under item #43, L310): on a **FactionSpecific** card, `faction.acting.native` always resolves to the same type, since `Card().faction` is fixed — write the bare `ResourceType` instead (e.g. a Network card writes `Exposure`, not `faction.acting.native`). A **Standard** card (`faction = All`) can use either form depending on design intent — the bare `ResourceType` if the cost should be one specific resource regardless of who plays it, or `faction.acting.native` if it should track whichever resource is native to whoever plays it (corrected at L312 — Standard cards aren't restricted to the relative form the way the first pass at this rule implied). `faction.target.native`/`district.Y.native` stay relative on any card, any subtype — the resolved type genuinely depends on runtime targeting. Written into §6.3 as authoring guidance; folds into the same not-yet-swept scope below (many of the ~150 existing instances are FactionSpecific cards whose `faction.acting.native`/`resource.faction(acting).type` terms should collapse to bare `ResourceType` in the same pass, not just change call syntax).

**Status: Closed S148 (PM05 04-n189/04-n192, PM02 L318).** Executed as 6 parallel per-file agent passes (Standard, Guild, Ghost, Directorate, Network, Syndicate — Ring Modifiers/Part1_Core had zero instances). 219 confirmed `resource.faction(...)`/`resource.district(...)` cost/boost instances converted to the canonical form: bare `ResourceType` for concrete-type and literal-named-faction cases (a named faction in a cost field is definitionally that card's own acting faction), `faction.acting.native` collapsed to bare `ResourceType` on FactionSpecific cards per the confirmed collapse rule, `faction.target.native`/`district.Y.native` kept relative everywhere. Scope held strictly to `cost=`/`boost=` fields — `success`/`successcrit`/`failcrit` occurrences of the same old syntax were deliberately left untouched (different field, not this item's scope). Stale checklist-prose quotes of the old syntax updated to match (assessment text itself untouched). Verified per-file and corpus-wide: fence (776) and `Card()` (385) counts held exactly before/after; monolith regenerated. **One related fix applied directly (not part of the agent passes):** 3 `affinity=` clauses in Part2_Standard.md (STD.CA.1, a Network card, STD.CA.15-area) referenced the cost expression's own sub-path as `cost.resource.district(target).native`/`cost.resource.exposure`/`cost.resource.native` — now-orphaned syntax once the underlying `cost=` field changed. Corrected to mirror the new cost syntax (`cost.district.target.native`, `cost.Exposure`, `cost.faction.acting.native`) — same principle as the main sweep, just a sub-path the per-file briefs didn't anticipate. **Two genuine new findings surfaced during the sweep, not fixed (content decisions):** (1) **SYN.MOD.1 The Fixer** had a leftover bare-callable `Findings(1)` that should have been caught by #22's original sweep but wasn't — this one was unambiguous (no type judgment needed), so fixed directly to `Findings * 1` rather than left flagged. (2) **Ghost's FieldVerification card** (`resource.faction(target1) * 1 + resource.faction(target2) * 1`, ~line 1496-1497) has no type attribute at all on either term — genuinely ambiguous, not touched, logged as new item #50 below.

---

### 48. Category: `#### Outstanding Issues` placeholder missing corpus-wide — only 55 of 385 cards show the section at all

**Surfaced by:** direct request (S148, while removing a stray provenance note from SYN.CA.7's Outstanding Issues section) — Andy's direction: the section should stay present as a template/placeholder on every card, even when empty, so future authors (Claude/agy/lev) have a structural home for open questions rather than stuffing commentary into Design Rationale prose, checklist Notes, or code comments instead.

**Already the documented rule, just not followed:** Art 04 §5 (`Part1_Core.md` line 238) already states "An empty Outstanding Issues section means no blocking questions remain" — the schema has always intended the section to be a standard part of every card's template, present whether or not it has content. Corpus check: every one of the 385 cards has a `#### Design Rationale` section (confirmed 1:1 via grep — 25 Standard + 46 Guild + 133 Ring Modifiers + 44 each Directorate/Network/Syndicate + 49 Ghost), but only 55 across all 8 Part files also carry `#### Outstanding Issues` — the other 330 omit the section entirely rather than showing it empty.

**Not fixed:** adding the placeholder to 330 cards is a large, mechanical, corpus-wide insertion (same shape/scale as item #47's CostExpr sweep and 04-n188's comment hygiene sweep) — not attempted inline. One card (SYN.CA.7) was fixed as the immediate trigger case, showing the target form: `#### Outstanding Issues` header followed by bare `None` when there's nothing to report.

**Status: Closed S148 (PM05 04-n192/Phase 1 complete, PM02 L320).** Executed via a single verified script rather than per-file agents (the operation is fully deterministic — every card has exactly one `#### Design Rationale` and one `#### Status`, so "missing" is unambiguous: insert immediately before `#### Status` whenever `#### Outstanding Issues` isn't already present in that card's block). Dry-run tested against a copy of Part2_Standard.md first, diffed and formatting-verified against the SYN.CA.1 template before running corpus-wide. 329 cards fixed (20 Standard, 132 Ring Modifiers, 38 Guild, 37 Ghost, 34 Directorate, 36 Network, 32 Syndicate) with the bare `#### Outstanding Issues\n\nNone\n` placeholder — **real content was NOT backfilled from Design Rationale/checklist prose** (that's a content-judgment task, out of Phase 1's mechanical scope; a future pass could migrate genuine open questions already described elsewhere into this section per-card). Verified corpus-wide: `#### Design Rationale` / `#### Outstanding Issues` / `#### Status` counts now match exactly (385/385/385) in every file; fence (776)/`Card()` (385) counts unchanged; monolith regenerated. **This closes Phase 1 (04-n192) in full — all 4 sub-items done.**

---

### 49. Category: Target Profile as sole CA/PA target-declaration mechanism — CLOSED same session

**Surfaced by:** scoping 04-n192 (Phase 1 CostExpr sweep, S148) — reconciling why some CA/PA cards use bare `target` (STD.CA.1, DIR.PA.5) while others use qualified `target_district`/`target_faction` (DIR.CA.5, NET.PA.4, GUI.CA.9) surfaced a deeper unconfirmed question: the physical Target Profile component (Art 02 §8, DB:48) records "one or more of" target faction/district/object via single printed field-slots per type, but nothing confirmed how a `target1`/`target2`/`each_target` card (a card targeting two districts, two factions, or a variable set — real corpus instances on one Ghost card, one Guild card, one Network card) actually populates a physical component with no dedicated second slot.

**Andy's ruling (S148):** Target Profile is the sole target-declaration/enumeration mechanism for every Covert Operation and Public Act — no card targets any other way. Multi-target cards record the extra target(s) on the existing free-form "declared parameters" line (the same mechanism already documented for SYN.CA.11 Redline's card-specific value declaration) — no new printed field needed. ModReactCards are categorically outside this system: they fire off board-state events, not a declared target, so their targeting context is entirely the firing TriggerExpr's own `faction=`/`district=`/`ring=` metadata (already-confirmed §6.3 vocabulary), never a Target Profile entry.

**Status: Closed S148.** Corpus-checked before writing the rule: all 32 confirmed `resource.district(...)` cost instances (and the `target`-qualified `resource.faction(...)` instances alongside them) sit on `CovertOperation`/`PublicAct` cards only — zero on a ModReactCard — so no card content needed fixing, only documentation. Written into `02___Components.md` (Target Profile Gameplay Requirements) and `Part1_Core.md` §6.3 (CostExpr X/Y comment block). Fence (776)/`Card()` (385) counts unchanged. Full detail: PM02 L317.

---

### 50. Category: Ghost FieldVerification's dual-target cost has no type attribute — possible conflict with the "cost always paid by acting" rule, not just a notation gap

**Surfaced by:** the 04-n189/#47 CostExpr sweep (S148, Ghost file agent pass). FieldVerification's `cost` reads:

```
cost = Findings * 2 + Exposure * 1
     + resource.faction(target1) * 1
     + resource.faction(target2) * 1
     + IntelToken(about=faction(target1)) * 1
     + IntelToken(about=faction(target2)) * 1
```

(`target1`/`target2` are two named factions the card's own `restriction` requires Ghost to hold Intel Tokens on — see lines 1489-1491.) The two `resource.faction(target1)`/`resource.faction(target2)` terms have no type attribute at all — neither a concrete `ResourceType` nor `.native` — so they don't parse against any confirmed `CostExpr` form. Left untouched by the sweep per instructions (no type guessed).

**Why this might be more than a typing gap:** every other cross-faction cost reference in the corpus that names a `target`-style faction resolves *which type* is owed while still being paid by the acting faction (item #43's "prior economic embedding" rule, GHO.CA.4/NET.PA.5 precedent — always written with an explicit `.native`). This card's two terms have no `.native`, which leaves genuinely ambiguous whether the intent is (a) the same prior-embedding shape with a missing `.native` attribute (Ghost pays, resolving to target1's/target2's own native types), or (b) something that actually draws from target1's/target2's own pools — which item #43 already ruled out as a valid `cost` shape corpus-wide (Andy, S148: cost is always paid from acting's own pool, no exception).

**Status:** Deferred (S149) — Andy's call: FieldVerification is already 🚫 BLOCKED (GR 7.2b) pending a full redesign; not worth ruling on notation that redesign will replace anyway. Revisit as part of that redesign, not independently.

---

### 51. Category: Invalid `-=`/`+=` statement-as-value in `success`/`successcrit` — new instances beyond item #17's closed scope

**Surfaced by:** DIR.CA.5 Sanctioned Raid, checked directly (S149) while pulling its `boost=` pattern as precedent for #28's GHO.CA.8 fix. `success` reads `faction(acting).standing -= (1 + n_boost)`, `successcrit` reads `faction(acting).standing += (1 + n_boost)` — the exact same invalid statement-as-value defect item #17 closed (NET.MOD.2, SYN.MOD.9), just on a CovertOperation rather than a ModReactCard, and not part of that item's original scope. Not fixed here — found in passing, not part of this session's ruling batch, and this is signed-content-adjacent (Art 04 CA content, higher stakes than a ModReact fix) rather than a drop-in swap.

**Status: Closed (S150), folded into item #55's full sweep.** DIR.CA.5 fixed: `success`'s list-item `faction(acting).standing -= (1 + n_boost)` → `.remove(1 + n_boost)`; `successcrit` → `faction(acting).standing.add(1 + n_boost)`; `failcrit`'s list-item → `.remove(1 + n_boost)`. The corpus-wide grep this item asked for ("worth a corpus-wide grep for more instances beyond this one") found 111 more — see item #55 for the full sweep.

---

### 52. Category: `target_taxonomy` and `declared_params` were two names for the same physical Target Profile slot — consolidated into `target_freeform`

**Surfaced by:** Andy, S150, mid-session while reviewing the GHO.CA.15 Routing Override draft (Phase 5 / 04-n196's #29 pattern-setter card). Target Profile's physical component (Art 02 §8, DB:48) has exactly 4 addressable slots: faction, district, object, freeform. Card()'s schema had 5 targeting fields — `target_district`/`target_faction`/`target_object` (clean 1:1 mapping) plus BOTH `target_taxonomy` (typed `TaxonomyExpr`, "action taxonomy category... declared at Phase B") AND `declared_params` (typed `DeclaredParamsExpr`, "free-form parameters written on TP declared-parameters line") — two differently-named fields both describing the same physical freeform slot. `declared_params` was never even added to Part1_Core.md's canonical `Card()` class definition despite being the field 6 real cards actually used (04-n106, open since S111). Corpus check found `target_taxonomy` has exactly **one** real (non-`None`) usage anywhere in 385 cards — DIR.PA.6 Standing Injunction — versus `declared_params`'s healthier 6 real usages (GHO.CA.1 Pattern Match, GHO.CA.5 Misdirection, GHO.CA.15, GUI.MOD.10, SYN.CA.10, SYN.CA.11), confirming `target_taxonomy` was the thin, under-used field of the pair.

**Status: Closed (S150).** Merged into one field, `target_freeform` — repurposes `target_taxonomy`'s schema slot/position, absorbs `declared_params`'s actual real-world usage and purpose. Closes 04-n106 (by consolidation, not the originally-scoped straight addition). Corpus-wide rename executed: Part1_Core.md's `Card()` class definition and §6.2 data dictionary rewritten (not just renamed — description updated to describe the merged freeform-line concept); all 385 cards' `target_taxonomy`/`declared_params` occurrences renamed to `target_freeform` (129 + ~10 real instances); DIR.PA.6's one real `target_taxonomy` usage preserved as freeform content (`taxonomy.declared`) rather than a distinct typed field, with its historical provenance note corrected to record the original S131 introduction accurately rather than being silently overwritten by the rename. `design_reference_card_system.md` and `ca_pa_review_notes.md` updated to match. Verified: code-fence counts unchanged across all 8 Part files post-rename, monolith regenerated, zero leftover `declared_params`/`target_taxonomy` occurrences outside intentional historical-provenance notes.

---

### 53. Category: `target_freeform` (formerly `declared_params`) used on ModReactCards — schema says CA/PA only

**Surfaced by:** Andy, S150, immediately after raising item #52 — asked "not sure how a ModBattleCard has any target" while reviewing a `declared_params` usage that turned out to be on a ModReactCard, not a ModBattleCard as first suspected. `design_reference_card_system.md`'s own confirmed rule: "This entire mechanism [Target Profile] is CA/PA-only — ModReactCards never use Target Profile; their targeting context comes from the firing TriggerExpr's own `faction=`/`district=`/`ring=` parameters instead." Two ModReactCards violate this: **GUI.MOD.10 Contractor's Favor** (`target_freeform = direction.named` — lets Guild declare Support/Withhold at trigger time) and **Overture / STD.MOD.1** (`target_freeform = None` currently, but the field is declared at all, which shouldn't happen on a ModReactCard by the documented rule).

**Status:** Open, 2 confirmed instances. Andy's direction (S150): flag, don't fix as part of the #52 rename — a ModReactCard-time player-declaration mechanism (distinct from CA/PA's Target-Profile-based declaration) doesn't exist yet in confirmed vocabulary, and deserves its own design pass rather than a rename-driven patch. Field name preserved as `target_freeform` on both cards for now (mechanically renamed along with everything else), but the underlying scope violation is unresolved.

---

### 54. Category: `schema_cleanup_log`/PM05 `04-n###` citations found scattered inside `Card()` python blocks (`design_note`/`arbiter_note`/inline comments) — corpus-wide sweep

**Surfaced by:** Andy, S150, after seeing the `(schema_cleanup_log #39)` note added to the DIR.PA.4/5/9/10/11 checklist rows during item #39 — asked whether other Card()-internal instances existed and whether a full corpus sweep was warranted. This is a direct extension of the earlier hygiene rule (S146/S147/S150): a `Card()` block is the actual game content — an ARBITER or player-facing spec — and has no business carrying process/punch-list citations, regardless of whether the citation is a bare session tag or a fact-paired reference.

**Scope decided (S150):** two-question check-in with Andy before touching anything. (1) Corpus scan found ~127 in-fence instances across `design_note`/`arbiter_note`/inline `#` comments (Part2/Standard: 0; Part3/Ring Mod: 39; Guild: 14; Ghost: 14; Directorate: 22; Network: 11; Syndicate: 10; Part1/Core: 1 real instance — see below) versus **~1,100+** instances in checklist Notes/Outstanding Issues/Status-footer italic lines. (2) Sampling the "outside" category showed those citations are structurally different from what was originally flagged — they're the load-bearing gating mechanism the whole review methodology depends on (`⚠ Pending 04-n70`, `Gates Art 03 re-sign-off`, `Sign-off gates: 04-n81...`), not decorative session narration. Andy's ruling: sweep `Card()` blocks only; leave every checklist/Outstanding Issues/Status-footer citation untouched — those keep doing real tracking work.

**Part1_Core carve-out:** Part1_Core.md's §6.1–§6.3 fenced content is the abstract `class Card` schema definition and vocabulary reference (not a card instance) — a different category again from both "game content" and "review scaffolding." It's designer/reference-facing documentation about the schema's own evolution, so its citations (e.g. `resolution_type`'s "formalized from a free-form str, schema_cleanup_log #41") were left in place. Only Part1_Core's one genuine card instance (`GD01`) was swept (1 `design_note` citation removed).

**Status: Closed (S150).** All ~127 real Card()-instance citations removed across the 6 faction/ring files (Part3 Ring Mod: 39, Guild: 14 [2 missed on first pass, caught and fixed on verification — 2881/2882], Ghost: 14, Directorate: 22 [+1 caught on verification — line 610's `04-n84`], Network: 11, Syndicate: 10) plus Part1_Core's 1 real instance (GD01). Substance preserved throughout — citations dropped, the underlying fact/instruction stays (e.g. "Adjacency restriction per 04-n6" → "Adjacency restriction applies"; "Tracked at PM05 04-n170; remove this comment once resolved." → removed entirely, redundant with the field's own content). GUI.MOD.10's `target_freeform` comment (the #53 scope-violation flag) was rephrased to drop the `schema_cleanup_log #53` pointer while keeping the substantive flag ("flagged as an open schema question, not yet resolved") — the open question itself is still tracked via item #53 above, just not cited by number inside the card. Verified per-file: fence counts unchanged in every file, zero remaining in-fence citations corpus-wide except Part1_Core's intentionally-preserved schema-reference block. Monolith regenerated; corpus-wide fence count (776) and `Card()` instance count (386) both unchanged post-sweep. Checklist/Outstanding Issues/Status-footer citations (~1,100+ instances) deliberately not touched — out of scope per Andy's ruling, and functionally necessary.

---

### 55. Category: `-=`/`+=` statement-as-value in `success`/`fail`/`on_accept`/`on_decline` (and nested `game.world_condition(effect=...)` params) — full corpus sweep, item #17's original scope was incomplete

**Surfaced by:** PM05 04-n202 (schema_cleanup_log #51, DIR.CA.5), tackled S150. Fixing DIR.CA.5 required first running the corpus-wide grep item #51 itself asked for ("worth a corpus-wide grep for more instances beyond this one"). Initial anchored grep (fields starting a line) found 61 more instances beyond DIR.CA.5. A second, broader pass — catching continuation lines inside multi-line `success = (...)`/`on_accept = (...)` tuples and lists, which a line-start-anchored search misses entirely — found yet more: **111 total instances** fixed, corpus-wide: Standard 41, Syndicate 21, Guild 18, Directorate 13 (incl. DIR.CA.5 itself), Network 13, Ghost 5. Ring Modifiers had zero instances of the defect — it exclusively used `.add()`/`.remove()` already, which is what first suggested that form (not `-=`/`+=`) was the intended canonical one.

**Two categories deliberately excluded, confirmed as different constructs, not the same defect:** (1) `affinity` field entries (`ConditionalExpr` type, e.g. `faction(acting) == Ghost: threshold += 25`) — these describe a pre-resolution cost/threshold delta conditional on which faction submits a Standard card, a different field type and grammar from the runtime `MutationExpr` fields (`success`/`fail`/etc.) that were item #17/#51's actual subject. 17 instances left untouched in Part2_Standard.md. (2) Plain-English prose using `+=`/`-=` as readability shorthand inside `arbiter_note` strings or `#` comments (e.g. DIR.PA.10's arbiter_note: "Success: Directorate PS += N") — not a field value, not parsed as code, just a natural way to describe a delta in a sentence. 3 instances left untouched (Directorate line 1655, Syndicate lines 887/2345).

**Andy's ruling (S150):** `.add()`/`.remove()` is canonical, given both counts were close (65 vs. 49 at the time of the decision) and Ring Modifiers' 100% convention. This reopens item #17's "closed" status — that item's audit only found 2 instances and never ran a full corpus grep; the ruling was right, the scope wasn't complete.

**Status: Closed (S150).** All 111 instances converted, minimal-transform (operator swap only, field paths preserved exactly — matches confirmed precedent for `.mandate.add()`/`.remove()` already in the corpus). Two structural side-fixes made in passing since they were on the exact same lines being touched: Network's NET.CA.4-family cards at lines 464/2061 had bare comma-separated multi-mutations with no wrapping parens (`success = A -= 2, B += 1,` — already broken independent of the operator issue) — wrapped in parens to match the established `success = (A, B),` tuple convention used everywhere else. Ghost's Part4b_Ghost.md line 159 checklist Note (quoting the old `failcrit = standing -= 2` syntax) updated to match the new code. Verified: fence counts unchanged per file, corpus-wide fence count (776) and `Card()` instance count (386) unchanged after regenerating the monolith. Full instance list and per-file counts in session transcript; not reproduced here.

---

### 56. Category: `Resource(...)` wrapper-function cost notation — a fourth CostExpr style, undetected until item #20-C

**Surfaced by:** pulling the 10 cross-resource ModReactCards named in item #20-C to check their actual cost values (S150). GHO.MOD.6 (`cost = Resource(faction(trigger.faction).native, 1)`), GHO.MOD.8 (identical shape), and GUI.MOD.6 (`cost = list([Resource(Capacity, 1), Resource(Capital, 1)])`) all use a `Resource(type, n)` wrapper-function call — not the bare `ResourceType * n` / `faction.X.native * n` grammar confirmed as canonical CostExpr in §6.3 (item #22, S148). This is a fourth coexisting cost-notation style, on top of the three item #22 already catalogued (`resource.faction(X).type * n`, bare `IntelToken(...) * 1`, bare `Type(n)`).

**Sharper wrinkle on GHO.MOD.6/8 specifically:** their cost isn't just non-canonical notation — it's a *dynamic* native reference, `faction(trigger.faction).native`, meaning the required resource type is whichever type the *triggering* faction (not Ghost) natively generates. That's a materially different cost shape from every other cross-resource card in the corpus: the other 8 named in #20-C have a fixed, printable cross-resource requirement (e.g. "1 Capacity"), while GHO.MOD.6/8's actual cost varies by who triggers the card and can't be stated on the card face as a fixed value.

**Status:** Open, flagged not fixed — out of scope for item #20-C (a design-economy question, not a notation question). Candidate direction: confirm whether `Resource(type, n)` should normalize to the canonical bare form (`Capacity * 1`, `faction(trigger.faction).native * 1`), and separately decide whether GHO.MOD.6/8's variable-type cost is an intentional design pattern (printable as "1 of the triggering faction's native resource") or needs reworking into a fixed-type cost.

---

### 57. Category: `district=district(trigger.target)` — a trigger referencing its own not-yet-matched value

**Surfaced by:** re-inspecting GUI.MOD.1 during item #20-A (S150). Its trigger reads `presence_chip.removed(faction=Guild, district=district(trigger.target))` — the district parameter of the trigger references `trigger.target`, a value that would only exist once the trigger has already matched something. Every other confirmed trigger in the corpus that needs to refer to "whichever district actually matched" does so from a *downstream* field (`target_district = trigger.district`, per CostExpr's own `faction.X.native`/`district.Y.native` pattern) — never from inside the trigger's own match condition. Not a one-off: GUI.MOD.10 uses the identical `district(trigger.target)` construct in its own trigger, so this is a repeated (if narrow) pattern, not a single typo.

**Status: Closed (S150).** Andy's read: correct intent, wrong syntax. ModReactCard carries no Target Profile, so its `target_district`/`target_faction`/`target_object` fields inherit directly from whatever the triggering event itself matched — that inheritance mechanism is real and needed, but it already exists in the corpus under a different, correct name: `trigger.district`. NET.MOD.1 already used this exact broken self-reference shape and was normalized to `target_district = trigger.district` at S149 (item #15) — direct precedent, not a new invention. GUI.MOD.1 and GUI.MOD.10 normalized to match: trigger's own district filter dropped entirely (matches any district, per the established "omit the parameter" convention already used by NET.MOD.1/`board_state.changed`), `target_district` reads `trigger.district`. Written into Part1_Core.md §6.3 as confirmed vocabulary — "ModReactCard target inheritance" — stating the general rule (no Target Profile → target_* fields inherit from `trigger.district`/`trigger.faction`/`trigger.object`) and the two failure modes it corrects (self-referencing the trigger's own not-yet-matched value inside its own condition; wrapping the downstream accessor in a redundant call instead of using it bare). Fence count (776) and `Card()` count (386) unchanged; monolith regenerated.

---

### 58. Category: multi-mutation notation — bare tuple `(A, B)` and `list([A, B])` coexist, no confirmed single form

**Surfaced by:** fixing SYN.MOD.8's semicolon-joined string (item #20-D, S150). The confirmed precedent cited for that fix, GHO.MOD.7/NET.MOD.10, uses `list([A, B])`. But NET.CA.7's `successcrit` already uses a bare tuple `(A, B)` for the identical "two mutations from one field" shape — two notations for the same thing, neither formally confirmed over the other in §6.3.

**Status:** Open, flagged not fixed. Candidate direction: pick one form as canonical (likely `list([...])`, given it has the clearer precedent so far) and sweep the corpus for the other — scope not yet counted.

---
